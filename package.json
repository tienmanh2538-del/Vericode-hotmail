{
  "name": "verification-tool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run tests/e2e",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build",
    "db:validate": "prisma validate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "worker:email": "tsx scripts/run-email-worker.ts",
    "worker:delta": "tsx scripts/run-delta-polling-worker.ts",
    "worker:delta:once": "tsx scripts/run-delta-polling-worker.ts --once",
    "worker:renewal": "tsx scripts/run-subscription-renewal-worker.ts",
    "worker:renewal:once": "tsx scripts/run-subscription-renewal-worker.ts --once"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "bullmq": "^5.77.6",
    "next": "14.2.35",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "dotenv-cli": "^11.0.0",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.35",
    "prisma": "5.22.0",
    "tsx": "^4.19.2",
    "typescript": "5.5.3",
    "vitest": "2.1.9"
  }
}
