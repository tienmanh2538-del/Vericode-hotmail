\# ROADMAP.md



\## Lưu ý vai trò reviewer (từ TASK-076)



Từ **TASK-076** trở đi, reviewer/tester độc lập của project là **Antigravity CLI**

(thay cho Gemini CLI). Source-of-truth: `ANTIGRAVITY.md`, `AGENTS.md`,

`docs/SECURITY_RULES.md`. Mọi mention "Gemini review PASS" trong các task/report

CŨ là **lịch sử thật** (task đó được Gemini review) và KHÔNG bị viết lại.



\## Sprint 0 — Foundation \& quality gates



\- TASK-001: Khởi tạo project foundation

\- TASK-002: Setup lint/typecheck/test/build/verify

\- TASK-003: Setup environment config \& secret safety

\- TASK-004: Setup database ORM \& initial schema



\## Sprint 1 — Admin shell \& access control



\- TASK-005: Tạo admin layout cơ bản

\- TASK-006: Tạo authentication/admin role skeleton

\- TASK-007: Tạo customer management tối giản



\## Sprint 2 — Telegram validation



\- TASK-008: Setup Telegram bot config \& test-send service

\- TASK-009: Tạo Telegram mapping module



\## Sprint 3 — Parser \& mock flow



\- TASK-010: Tạo mock email input

\- TASK-011: Tạo Facebook/Meta verification detector

\- TASK-012: Tạo code extractor module

\- TASK-013: Tạo processed message \& deduplication service

\- TASK-014: Kết nối mock flow: mock email → detect → extract → dedupe → Telegram



\## Sprint 4 — Logs \& audit



\- TASK-015: Tạo code event log page

\- TASK-016: Tạo audit log service \& page



\## Sprint 5 — Microsoft OAuth validation



\- TASK-017: Tạo Microsoft App Registration checklist \& config

\- TASK-018: Tạo Microsoft OAuth connect URL

\- TASK-019: Tạo Microsoft OAuth callback

\- TASK-020: Tạo token encryption service

\- TASK-021: Lưu mailbox sau OAuth connect

\- TASK-022: Tạo Microsoft Graph mail service: read Inbox test



\## Sprint 6 — Microsoft webhook \& worker



\- TASK-023: Tạo Graph subscription service

\- TASK-024: Tạo Microsoft webhook verification endpoint

\- TASK-025: Tạo webhook receiver cho notification thật

\- TASK-026: Setup queue \& worker foundation

\- TASK-027: Worker xử lý Graph message → detector → extractor → Telegram



\## Sprint 7 — Mailbox dashboard



\- TASK-028: Tạo mailbox list page

\- TASK-029: Tạo mailbox detail page

\- TASK-030: Tạo connect mailbox UI ( (Đã làm sớm một phần ở TASK-026 để test luồng: Nút Connect & Banner lỗi
     tại Admin Dashboard). Khi tới Sprint 7 chỉ cần review và tinh chỉnh nếu cần.)



\## Sprint 8 — Reliability



\- TASK-031: Tạo delta polling backup worker

\- TASK-032: Tạo subscription renewal worker

\- TASK-033: Tạo Telegram retry \& failure handling

\- TASK-034: Tạo health dashboard
  - Note from TASK-031: verify production email-worker is wired to a real pipeline (createEmailWorker hiện default chỉ là type-only cast, không thực sự chạy production) và surface trạng thái qua dashboard / operational check.

\- TASK-035: Tạo alert service



\## Sprint 9 — Security \& staging readiness



\- TASK-036: Security hardening review
lưu ý:   - Tối ưu hóa Delta Polling: Hỗ trợ bootstrap mailbox cực lớn (dùng $filter thời gian hoặc tăng max pages cho
      lần chạy đầu).
    5   - Sửa lỗi Token Rotation: Đảm bảo service cấp mới Access Token (ví dụ `refresh-access-token.service.ts`) có
      lưu lại Refresh Token mới xuống database nếu Microsoft trả về (hiện tại có rủi ro mất token nếu bị cấp mới).

\- TASK-037: E2E test cho mock flow

\- TASK-038: E2E test cho Microsoft test mailbox
  - Note from TASK-031: cover trường hợp webhook + delta polling cùng thấy 1 graphMessageId — Telegram phải nhận đúng 1 lần (dựa vào ProcessedMessage unique [mailboxId, graphMessageId]).

\- TASK-039: Staging deployment setup
\- TASK-040 Preflight: Operational Readiness Before MVP Acceptance Review

\- TASK-040: MVP acceptance review
\- TASK-041-flexible-telegram-routing.md

## Nguyên tắc bắt buộc cho toàn bộ lộ trình internal production (Sprint 10 → 15)

Các nguyên tắc sau áp dụng cho **mọi** task từ TASK-042 đến TASK-061:

1. **Mỗi mailbox chỉ có một active Telegram group/topic destination.** Khi gửi verification
   code, mailbox resolve đúng **một** destination active của chính nó.
2. **KHÔNG làm 1 mailbox → nhiều Telegram destination.** Hướng này nằm ngoài scope toàn bộ
   lộ trình. (TASK-041 cho phép NHIỀU mailbox → CÙNG một group/topic một cách có chủ đích,
   nhưng một mailbox vẫn chỉ có một destination.)
3. **Microsoft publisher verification KHÔNG phải blocker hiện tại.** Chỉ theo dõi; chỉ xử lý
   nếu/khi consent thực tế bị chặn (ví dụ tenant yêu cầu admin consent, hoặc `AADSTS65001` /
   consent required). Tham chiếu `docs/MICROSOFT_SETUP.md`.

## Sprint 10 — Internal production readiness

- TASK-042: Internal production readiness plan
- TASK-043: Prisma Client generation hardening
- TASK-044: Confirm one-mailbox-one-destination routing rule

## Sprint 11 — Staff operation model

- TASK-045: Internal staff ownership & assignment model — done. Confirms this is an internal staff app: OWNER/ADMIN see everything; STAFF_READ_ONLY sees only customers assigned to them (and those customers' mailboxes/mappings). Customers never log in.
- TASK-046: Staff dashboard UX for high mailbox volume
- TASK-047: Safe mailbox onboarding flow

## Sprint 12 — Staging deployment

- TASK-048: Choose deployment platform & staging architecture — quyết định: Railway (khuyến nghị), Render (dự phòng), Vercel-only không phù hợp do worker long-running. Xem `docs/reports/TASK-048-choose-deployment-platform-staging-architecture.md`.
- TASK-049: Staging infrastructure setup
- TASK-050: Microsoft App Registration staging validation — done. Đã xác minh ở mức docs/checklist: App Registration staging riêng, Redirect URI + Webhook URL staging (placeholder), permission tối thiểu (Mail.Read, offline_access, User.Read), an toàn client secret. Xem `docs/tasks/TASK-050-microsoft-app-registration-staging-validation.md`, `docs/reports/TASK-050-microsoft-app-registration-staging-validation.md`. Rủi ro còn lại: chưa chạy live mailbox E2E → để TASK-051.
- TASK-051: Staging live mailbox E2E test — **pre-live staging validation pass (conditional)**. Đã PASS trên staging: login/logout, /admin/health smoke check, customer + mailbox assignment UI, Telegram mapping UI + test-send, Mock Email dropdown + Process & send to Telegram, API scope check cho mock-email process. Live Microsoft email path (webhook / delta polling / duplicate bằng email thật) **deferred** sang internal beta / product trial vì hiện chưa có email thật phù hợp. Xem `docs/reports/TASK-051-staging-live-mailbox-e2e-test.md` (mục 6 + 6.1). Trước khi chạy phần live còn lại, đọc `docs/MICROSOFT_SETUP.md`, `docs/STAGING_DEPLOYMENT.md`, và task/report TASK-050.
- TASK-052: Safe mailbox disconnect flow — done. OWNER/ADMIN có thể disconnect mailbox an toàn mà không hard delete mailbox hoặc xóa lịch sử xử lý. Mailbox disconnected không còn được poll, renew subscription, hoặc relay code. Active Telegram mapping liên quan được chuyển khỏi trạng thái active để tránh relay nhầm. Graph subscription cleanup được xử lý fail-safe: nếu remote cleanup lỗi, local state vẫn chặn relay. Xem docs/tasks/TASK-052-safe-mailbox-disconnect-flow.md và docs/reports/TASK-052-safe-mailbox-disconnect-flow.md. Rủi ro còn lại: nếu có email thật đã enqueue trước khi disconnect, worker/pipeline phải dựa vào re-check mailbox status để bỏ qua; cần tiếp tục xác minh trong internal beta/live Microsoft email test.
- TASK-053: Reusable Telegram destinations
 Reusable Telegram destinations — done. Added reusable Telegram destinations so Owner/Admin can configure a Telegram group/topic once and reuse it across mailbox mappings. Many mailboxes can share one destination, while each mailbox still has at most one active destination. Customer isolation is enforced when mapping mailbox to destination. See `docs/tasks/TASK-053-reusable-telegram-destinations.md` and related task report if created. Remaining risk: keep future mapping changes going through service-layer validation to avoid bypassing customer isolation.

## Sprint 13 — Scale readiness

- TASK-054: Scale test plan for 100–200 mailboxes per staff — done (planning artifact). Scale test plan / readiness plan an toàn cho mức 50/100/200 mailbox per staff bằng dữ liệu giả lập (mock/seed/staging fake), không live scale, không production resource, không mailbox/Telegram group khách hàng thật, không gửi code thật. Báo gồm baseline (worker concurrency mặc định 2, delta polling 30s / max 10 pages, subscription renewal 15 phút, Telegram retry 4 lần backoff 5/15/30s), test data model, scenarios A–E, metrics (queue backlog, worker latency, delta polling, Graph throttling, Telegram failure, UI, readiness), risk analysis, safe execution checklist. Nhu cầu per-mailbox/per-destination throttling & queue safety chuyển TASK-055; observability realtime chuyển TASK-056. Xem `docs/tasks/TASK-054-scale-test-plan-100-200-mailboxes.md` và `docs/reports/TASK-054-scale-test-plan-100-200-mailboxes.md`.
- TASK-055: Per-mailbox throttling & queue safety — done (minimal implementation). Thêm per-mailbox processing lock (một mailbox không xử lý Graph/Telegram song song; lease tự hết hạn; release trong finally; job bận defer có giới hạn, không retry vô hạn) và shared-destination burst guard (giãn cách gửi tới cùng group/topic bằng delay nội tuyến có giới hạn, không broadcast, không đổi routing). Mailbox disconnected skip trước Graph, mailbox chưa có active mapping hợp lệ skip trước Telegram. Giữ nguyên rule nhiều mailbox dùng chung một reusable destination và rule mỗi mailbox chỉ có một active destination; giữ customer isolation. `npm run verify` PASS; không log token/secret/full code/full email body. Xem `docs/tasks/TASK-055-per-mailbox-throttling-queue-safety.md` và `docs/reports/TASK-055-per-mailbox-throttling-queue-safety.md`. Follow-up: thêm test coverage cho graph message pipeline throttling (`tests/unit/email/graph-message-pipeline.throttling.test.ts`) bảo vệ per-mailbox lock và shared-destination throttle; Gemini review PASS, `npm run verify` PASS — đây là follow-up coverage, không mở rộng scope. Rủi ro còn lại: lock chỉ serialize trong một tiến trình worker (đa tiến trình cần lock chia sẻ qua hạ tầng ngoài), hằng số throttle/TTL còn ở mặc định trong code, và chưa xác minh tải thật với mailbox/Telegram group thật.
- TASK-056: Operational health dashboard for staff workload — done (minimal implementation). Nâng cấp `/admin/health` thành dashboard vận hành tối thiểu với customer scope enforce ở service layer: OWNER/ADMIN thấy toàn bộ, STAFF_READ_ONLY chỉ thấy mailbox của customer được phân công (mọi count/aggregate đều scope theo mailbox, fail-closed khi không có assignment), operational/infra checks toàn hệ thống chỉ hiển thị cho OWNER/ADMIN. Thêm overview cards (Ready / Needs mapping / Needs customer / …), Workload by customer, và mailbox issues table dùng chung định nghĩa readiness (mailbox disconnected không bao giờ Ready; ACTIVE thiếu mapping → Needs mapping; nhiều mailbox dùng chung reusable destination vẫn hợp lệ). Read-only, không gọi Microsoft Graph/Telegram để render; không log/hiển thị token/secret/full code/full email body. `npm run verify` PASS. Xem `docs/tasks/TASK-056-operational-health-dashboard-staff-workload.md` và `docs/reports/TASK-056-operational-health-dashboard.md`. Rủi ro/deferred còn lại: chưa có queue backlog/worker heartbeat thật (hiển thị Unknown/Degraded an toàn, không tự probe external); ngưỡng "recent issue" tạm dùng tổng Telegram fail 24h; scope đa-tiến-trình phụ thuộc baseline TASK-055.

## Sprint 14 — Production security & internal launch

- TASK-057: Production auth hardening for internal staff — done. Gemini review PASS, `npm run verify` PASS. Kết quả chính đã chốt: production fail-closed tường minh (admin access ở production luôn bị từ chối khi chưa có sign-in provider thật — không dùng demo user dev, không dùng staging passphrase login); role/userId chỉ lấy từ session đã verify server-side, không lấy từ request/cookie chưa verify; staging session cookie giữ httpOnly/secure/sameSite Lax/expiry và logout xóa đúng; RBAC OWNER/ADMIN vs STAFF_READ_ONLY và customer assignment scope vẫn enforce ở service/API layer; login page production không lộ tên biến môi trường hay cơ chế; thêm audit tối thiểu cho login/logout/access-denied (metadata an toàn). Khách hàng vẫn không có login/portal/signup. Không phá routing Telegram, disconnect guard (TASK-052), throttling/queue safety (TASK-055), health dashboard (TASK-056). Xem `docs/tasks/TASK-057-production-auth-hardening-internal-staff.md` và `docs/reports/TASK-057-production-auth-hardening-internal-staff.md`. Rủi ro còn lại: chưa có production sign-in provider thật (cố ý — admin access production khóa an toàn cho tới khi TASK-058 thêm provider); audit là best-effort nên có thể không ghi khi store không sẵn sàng nhưng luồng auth vẫn an toàn; scope đa-tiến-trình vẫn theo baseline TASK-055. Bàn giao tiếp cho TASK-058 (production environment & secret setup) rồi TASK-059 (production deployment limited internal beta).
- TASK-058: Production environment & secret setup — docs/placeholder standardization done (chưa deploy production, không tạo/dùng DB/Redis/mailbox/Telegram group thật). Chuẩn hóa danh sách biến môi trường production theo nhóm và đồng bộ giữa `.env.example`, `lib/env.schema.ts`, `deployment/staging/env.staging.example`, `docs/STAGING_DEPLOYMENT.md`; vá placeholder còn thiếu cho biến delta-polling bootstrap lookback (đã có trong code, default an toàn). Production auth vẫn fail-closed theo TASK-057 (không thêm cơ chế login production mới). Giữ nguyên internal staff app, RBAC OWNER/ADMIN vs STAFF_READ_ONLY + assignment scope, one-mailbox-one-destination, reusable destinations, disconnect guard, throttling/queue safety, health dashboard. `npm run verify` PASS. Xem `docs/tasks/TASK-058-production-environment-secret-setup.md` và `docs/reports/TASK-058-production-environment-secret-setup.md`. Còn lại: quan sát secret-hygiene về giá trị `ENCRYPTION_KEY` mẫu trong `.env.example` (đang là test fixture) được defer; chờ Gemini review trước khi chốt. Bàn giao tiếp cho TASK-059.
- TASK-059: Production deployment limited internal beta — done (Gemini review PASS, `npm run verify` PASS). Đây là **limited internal beta**, không phải full production rollout: chuẩn bị tài liệu/checklist deploy production tối thiểu (web + database + Redis + 3 worker email/delta/renewal), thêm `deployment/production/README.md` và `deployment/production/env.production.example` placeholder-only; xác minh deployment checklist chỉ dùng lệnh có thật trong `package.json` và `npx prisma migrate deploy` (không `migrate dev`). Chỉ thêm docs, không sửa runtime code. Giữ nguyên internal staff app (không public SaaS, không customer login/portal/signup, không billing/payment, không scale-up hàng loạt — scale-up để TASK-063), RBAC OWNER/ADMIN xem toàn bộ vs STAFF_READ_ONLY chỉ xem customer/mailbox được assigned, reusable Telegram destinations + rule mỗi mailbox tối đa một active destination, disconnect guard (TASK-052), throttling/queue safety (TASK-055), health dashboard (TASK-056), production auth hardening (TASK-057), production env/secret setup (TASK-058). File liên quan: `docs/tasks/TASK-059-production-deployment-limited-internal-beta.md`, `docs/reports/TASK-059-production-deployment-limited-internal-beta.md`, `deployment/production/README.md`, `deployment/production/env.production.example`. Rủi ro/blocker còn lại: production auth vẫn fail-closed vì chưa có sign-in provider production thật → staff-facing beta chưa usable qua admin UI (deploy/infra-verifiable, không bypass bằng staging/dev login); live beta với email/Telegram thật chưa chạy. Bàn giao tiếp cho TASK-060 (backup, restore & incident response).
- TASK-060: Backup, restore & incident response — done (Gemini review PASS, `npm run verify` PASS). Docs-only runbook cho **production limited internal beta**, không phải full rollout. Kết quả chính: thêm runbook gồm backup strategy tối thiểu (backup do deploy/database provider quản lý, không commit/không upload vào chat AI), restore drill an toàn vào **môi trường tách biệt** (không trỏ production web/worker vào restore-test, không bật worker, không dùng bot production, không connect mailbox thật, không gửi code thật), và incident response cho: deploy/build/migration (chỉ `prisma migrate deploy`, không `migrate dev`, không sửa DB tay), database, Redis/queue (Redis không phải nguồn dữ liệu chính — phục hồi qua database state + delta polling), worker crash hoặc worker chạy sai môi trường, Microsoft OAuth/Graph/subscription, Telegram gửi thất bại hoặc gửi nhầm destination, nghi lộ secret, verification code hoặc email body bị log nhầm, auth/session, và emergency worker shutdown **không xóa dữ liệu** (thứ tự email → delta → renewal, giữ database/Redis nguyên trạng). Chỉ thêm/sửa docs, không sửa runtime code, không sửa `.env*`, không sửa GitHub Actions; giữ nguyên internal staff app, RBAC OWNER/ADMIN vs STAFF_READ_ONLY + assignment scope, reusable destinations + rule mỗi mailbox tối đa một active destination, disconnect guard (TASK-052), throttling/queue safety (TASK-055), health dashboard (TASK-056), production auth fail-closed (TASK-057), production env/secret setup (TASK-058), limited beta guardrails (TASK-059). File liên quan: `docs/tasks/TASK-060-backup-restore-incident-response.md`, `docs/reports/TASK-060-backup-restore-incident-response.md`, `deployment/production/README.md` (thêm link ngắn tới runbook). Scope **không** gồm staff onboarding, daily operations checklist, hay full scale-up. Rủi ro/blocker còn lại: kế thừa từ TASK-059 — chưa có production sign-in provider thật nên staff beta chưa usable qua admin UI; restore drill mới ở mức hướng dẫn, chưa chạy thật. Deferred: TASK-061 (staff onboarding guide), TASK-062 (daily operations checklist), TASK-063 (production scale-up from beta to full internal use).

## Sprint 15 — Internal operations

- TASK-061: Staff onboarding guide — done (Gemini review PASS, `npm run verify` PASS). Docs-only staff onboarding guide cho **internal staff** trong limited internal beta. Guide hướng dẫn cho người không chuyên code: login expectation (production hiện fail-closed, phụ thuộc sign-in provider từ task trước), phân biệt role OWNER/ADMIN (xem toàn bộ) vs STAFF_READ_ONLY (chỉ customer/mailbox được assign), customer assignment, mailbox readiness, connect mailbox, reusable Telegram destination mapping, safe test-send, logs/health ở mức cơ bản, và khi nào báo OWNER/ADMIN. Giữ nguyên internal staff app: không customer login, không public signup, không billing/payment; nhiều mailbox dùng chung một reusable destination nhưng mỗi mailbox chỉ một active destination, không broadcast, mailbox disconnected hoặc chưa mapping hợp lệ không Ready. Chỉ thêm docs, không sửa runtime code, không sửa `.env*`, không sửa GitHub Actions. **Không** lấn sang TASK-062 (daily operations checklist) hay TASK-063 (production scale-up). File liên quan: `docs/operations/STAFF_ONBOARDING_GUIDE.md`, `docs/tasks/TASK-061-staff-onboarding-guide.md`, `docs/reports/TASK-061-staff-onboarding-guide.md`. Rủi ro còn lại: production staff-facing beta vẫn phụ thuộc trạng thái production sign-in provider từ task trước (TASK-057/TASK-059); daily routine để TASK-062; full scale-up để TASK-063.
- TASK-062: Daily operations checklist — done (Gemini review PASS, `npm run verify` PASS). Docs-only daily operations checklist cho **OWNER/ADMIN** và **STAFF_READ_ONLY** trong production limited internal beta. Checklist hướng dẫn vòng kiểm tra đầu ngày: health dashboard, mailbox readiness (xác nhận disconnected hoặc chưa mapping → không Ready), Telegram send failure, logs cơ bản, worker/queue/subscription/token signals (nếu dashboard hỗ trợ), và escalation (khi nào báo OWNER/ADMIN, khi nào OWNER/ADMIN dùng runbook TASK-060). Giữ nguyên internal staff app: không customer login, không public signup, không billing/payment; giữ rule reusable Telegram destinations + mỗi mailbox tối đa một active destination. Không thay thế incident runbook (TASK-060) và không trùng onboarding guide (TASK-061). Chỉ thêm docs, không sửa runtime code, không sửa `.env*`, không sửa GitHub Actions. File liên quan: `docs/operations/DAILY_OPERATIONS_CHECKLIST.md`, `docs/tasks/TASK-062-daily-operations-checklist.md`, `docs/reports/TASK-062-daily-operations-checklist.md`. Rủi ro còn lại: chưa chạy thử qua chu kỳ vận hành thật với email/Telegram thật. Tiếp theo: TASK-063 Production scale-up from beta to full internal use.
- TASK-063: Production scale-up from beta to full internal use — done (Gemini review PASS, `npm run verify` PASS). Docs-only planning/checklist để mở rộng production từ limited internal beta sang full internal use **theo từng giai đoạn**. Kết quả chính: định nghĩa các mức scale (Level 0 limited beta → Level 1 expanded beta → Level 2 internal scale pilot → Level 3 full internal use) với điều kiện PASS trước mỗi mức, metric cần theo dõi (health dashboard, mailbox readiness, Telegram failures, worker/queue, Graph throttling, token/subscription, latency, duplicate prevention, staff scope), và rollback criteria. Có hướng dẫn dùng lại daily operations checklist (TASK-062) làm nhịp kiểm tra trong mỗi đợt scale và incident runbook (TASK-060) khi scale gây sự cố; không thay thế hai tài liệu đó. Giữ nguyên internal staff app: không customer login, không public signup, không billing/payment; nhiều mailbox dùng chung một reusable destination nhưng mỗi mailbox chỉ tối đa một active destination, không broadcast; mailbox disconnected/unmapped không Ready; STAFF_READ_ONLY chỉ xem customer/mailbox được assign. Chỉ thêm docs, không sửa runtime code, không sửa `.env*`, không sửa GitHub Actions. File liên quan: `docs/tasks/TASK-063-production-scale-up-full-internal-use.md`, `docs/operations/PRODUCTION_SCALE_UP_CHECKLIST.md`, `docs/reports/TASK-063-production-scale-up-full-internal-use.md`. Rủi ro còn lại: cần chạy scale thật theo từng giai đoạn và theo dõi metric vận hành trước khi tăng lên full internal use.

## Test phase 1 — Final technical audit

- TASK-064: Final technical audit & phase 1 test review — **CONDITIONAL PASS** (Gemini review CONDITIONAL PASS, `npm run verify` PASS: 857 tests, lint/typecheck/build sạch). Đã hoàn tất vai trò technical audit phase 1 (audit + automated test/review; CHƯA test UI sâu, CHƯA tối ưu UI — để phase 2). Không sửa runtime code. Không có finding Critical; các business rule cốt lõi (RBAC + scope, routing one-active + reusable destination, pipeline dedup, disconnect safety, queue/throttle/retry hữu hạn, secret/token/code masking, production auth fail-closed, production/staging safety) đều CONFIRMED ở service layer. **Backlog/risk bắt buộc khi chuyển phase 2:** H1 (High) — legacy Telegram mapping API `POST/PATCH /api/telegram/mappings[/id]` có thể bypass customer isolation khi dùng raw telegramChatId (hiện chỉ OWNER/ADMIN, không lộ qua UI), **phải fix trước production / staff real use**; M1 (Medium) — dedup race condition (read-then-write, chưa bắt unique-constraint), **phải xử lý trước khi chạy nhiều worker replica**; M2 (Medium) — one-active-mapping guard có TOCTOU risk, nên xử lý đầu phase 2. File liên quan: `docs/tasks/TASK-064-final-technical-audit-phase-1.md`, `docs/reports/TASK-064-final-technical-audit-phase-1.md`. **Task tiếp theo đề xuất: TASK-065 — Fix legacy Telegram mapping API customer isolation (H1) trước production / staff real use** (retire route legacy / route qua đường destination-based đã enforce isolation / thêm validation isolation + customer-scope, kèm test cross-customer cho mọi route mutation mapping).

## Test phase 2 — Backlog fixes

- TASK-067: Mailbox disconnect / Telegram mapping status consistency — done (Gemini review PASS, `npm run verify` PASS). Fix issue: mailbox DISABLED nhưng vẫn tạo/kích hoạt được ACTIVE Telegram mapping qua reusable destination → trang Telegram báo ACTIVE còn Mailboxes/detail báo Disabled, gây hiểu nhầm "mapping Active = mailbox Ready". Service layer giờ chặn tạo/đổi mapping sang ACTIVE cho mailbox DISABLED (vẫn cho mapping DISABLED để pre-stage); UI mailbox detail hiển thị rõ cần reconnect và bỏ checklist hard-code "mailbox đã connect". Giữ nguyên safety TASK-052: disconnected mailbox không poll, không renew subscription, không relay; giữ nguyên reusable destination và rule mỗi mailbox tối đa một active destination; gán mapping không tự re-enable mailbox. File liên quan: `docs/tasks/TASK-067-mailbox-disconnect-mapping-status-consistency.md`, `docs/reports/TASK-067-mailbox-disconnect-mapping-status-consistency.md`. Rủi ro còn lại: mapping ACTIVE cũ tạo trước guard có thể vẫn tồn tại trên mailbox DISABLED — an toàn về relay (mailbox DISABLED không relay) nhưng nếu muốn dọn dữ liệu cũ thì mở task vận hành riêng.
- TASK-066: Real operation UI QA & phase 2 test — checklist/template ready. Đây là **phase 2 real-operation UI QA** sau technical audit phase 1 (TASK-064) và fix H1 (TASK-065): chuẩn bị checklist + report template để test UI thực tế theo luồng mock/live-safe (safety precheck, UI smoke test mọi màn hình admin, mock email E2E, reusable destination, one-mailbox-one-active-destination, customer isolation/role scope, disconnect safety, health/logs/operations review) và ghi nhận issue UX/UI theo format chuẩn (ID, Screen, Severity, Steps, Expected, Actual, Screenshot, Suggested next task). **Không** dùng mailbox/Telegram group khách hàng thật ở bước đầu; **không** gửi code thật; chỉ thêm docs, không sửa runtime/UI code, không sửa `.env*`/GitHub Actions, không in secret. Nếu phát hiện UI/UX issues thì **mở task fix riêng** (đề xuất TASK-067+) sau khi review — task này không tối ưu UI. File: `docs/tasks/TASK-066-real-operation-ui-qa-phase-2.md`, `docs/reports/TASK-066-real-operation-ui-qa-phase-2.md`. Backlog kỹ thuật phase 2 (M1 dedup race, M2 TOCTOU one-active, M3 scope optional, L1 Telegram `description` log) vẫn theo dõi độc lập.
## Scale-readiness — Distributed safety & throughput

- TASK-068A: Distributed Safety & Exactly-Once Guarantees — done (Gemini review PASS, `npm run verify` PASS: 894 tests, lint/typecheck/build sạch). Đóng các blocker concurrency trước khi nghĩ tới test 100 mailboxes (xuất phát từ backlog M1/M2 của TASK-064). Kết quả chính: (1) dedup exactly-once — khi hai luồng (webhook + delta polling, hoặc nhiều worker replica) cùng thấy một `graphMessageId`, va chạm unique constraint lúc create giờ được coi là duplicate clean skip thay vì ném lỗi gây worker retry nhiễu; (2) thêm Redis-backed mailbox lock cross-process với TTL/lease và release an toàn bằng compare-and-delete (chỉ xóa khi còn đúng lease của mình), fail-safe khi Redis không sẵn sàng — production mặc định vẫn dùng in-memory lock nên không đổi behavior hiện tại; (3) thêm DB-level partial unique index bảo vệ rule mỗi mailbox chỉ có một ACTIVE Telegram mapping (chống TOCTOU), service map va chạm thành conflict thân thiện. Giữ nguyên reusable destinations và rule nhiều mailbox có thể dùng chung một destination, mỗi mailbox tối đa một active destination, không broadcast, không đổi routing; không log/ghi secret/token/full code/full email body; chỉ sửa runtime + test + migration + docs, không động `.env*`/GitHub Actions/package scripts. File liên quan: `docs/tasks/TASK-068a-distributed-safety-exactly-once.md`, `docs/reports/TASK-068a-distributed-safety-exactly-once.md`. Rủi ro còn lại: partial unique index không biểu diễn được đầy đủ trong `schema.prisma` nên cần lưu ý schema drift khi dùng `prisma migrate dev` (production dùng `migrate deploy`); muốn bật lock cross-process thật thì production cần inject/shared Redis client; và đây chưa đủ để test 100 mailboxes ngay — còn cần TASK-068B (throughput), TASK-068C (observability), và TASK-068D (100-mailbox readiness validation). **Task tiếp theo đề xuất: TASK-068B — Scale-Ready Queue Throughput.**
- TASK-068B: Scale-Ready Queue Throughput — done (Gemini review PASS, `npm run verify` PASS: 916 tests, lint/typecheck/build sạch). Điều tiết throughput **có giới hạn, cấu hình được** để chuẩn bị mốc ~100 mailboxes mà không nghẽn queue hoặc bị nhà cung cấp bóp tần suất. Kết quả chính đã chốt: (1) worker concurrency có clamp/cận trên an toàn (giữ default 2, cấu hình được, chặn set quá cao); (2) BullMQ/job rate limiter làm mịn burst job (default thận trọng, chỉ giãn thời điểm start — không fail/không retry vô hạn); (3) Telegram global bot pacing bên cạnh per-destination throttle (TASK-055), có cap thời gian chờ, không đổi routing/không broadcast/không multi-destination; (4) fairness/backoff có giới hạn cho mailbox busy để giảm retry/defer thrash (chặn bởi cả số lần lẫn tổng thời gian chờ — không vòng vô hạn, không giữ worker slot quá cap). Giữ nguyên exactly-once (TASK-068A) và routing/reusable destinations + rule mỗi mailbox tối đa một active destination; không log/ghi secret/token/full code/full email body; chỉ sửa runtime + test + docs, không động `.env*`/GitHub Actions/package scripts. File liên quan: `docs/tasks/TASK-068B-scale-ready-queue-throughput.md`, `docs/reports/TASK-068b-scale-ready-queue-throughput.md`. Rủi ro/deferred còn lại: chưa chạy validation 100 mailboxes thật (để TASK-068D); observability queue backlog / worker latency / throttle-defer signals (để TASK-068C); Low issue từ Gemini — tài liệu vận hành cho các biến throughput mới (vd `EMAIL_WORKER_RATE_MAX`) còn cần chuẩn hóa, **không** sửa trong 068B vì scope cấm động `.env*`. **Task tiếp theo đề xuất: TASK-068C — Observability for queue backlog, worker latency, throttle/defer signals.**
- TASK-068C: Observability for Queue Backlog, Worker Latency, Throttle/Defer Signals — done (`npm run verify` PASS: 944 tests, lint/typecheck/build sạch; chờ Gemini review). Thêm observability tối thiểu **read-only** để OWNER/ADMIN đánh giá độ sẵn sàng trước validation 100 mailboxes (TASK-068D), không chạy validation thật, không đổi routing/exactly-once (068A)/throughput (068B). Kết quả chính: (1) queue backlog snapshot từ BullMQ job counts (waiting/active/delayed/failed/completed) + backlog total + oldest waiting/delayed age + status OK/WARN/CRITICAL/UNKNOWN, có timeout, Redis/BullMQ lỗi → UNKNOWN không crash `/admin/health`, không in Redis URL; (2) worker latency instrumentation đo `queueWaitMs` (từ `job.timestamp`) + `processingDurationMs`, ghi aggregate khi job completed/failed/skipped/deferred — chỉ `{result, queueWaitMs, processingDurationMs}`, không payload/email/code/token; (3) throttle/defer signals aggregate cho mailbox-busy defer, destination throttle wait, global Telegram pacing wait, best-effort (metrics lỗi không phá delivery); (4) cross-process store dùng Redis hash bucket TTL ngắn (5 phút/bucket, giữ 60 phút) qua client BullMQ sẵn có (không thêm ioredis top-level), `max` atomic bằng Lua; (5) `/admin/health` hiển thị section "Queue & worker observability" read-only (không nút scale/retry/purge) chỉ cho OWNER/ADMIN (`scope.kind === 'all'`), STAFF_READ_ONLY nhận `null` không thấy global infra, Unknown/Degraded hiển thị rõ khi không đọc được. Không log/ghi secret/token/full code/full email body; chỉ sửa runtime + test + docs, không động `.env*`/GitHub Actions/migration. File liên quan: `docs/tasks/TASK-068C-observability-queue-backlog-worker-latency.md`, `docs/reports/TASK-068C-observability-queue-backlog-worker-latency.md`, `services/observability/*`. Rủi ro/deferred còn lại: infra loader phải được page inject (default `loadHealthDashboard` dùng null-loader để unit test DB không chạm Redis); metrics best-effort nên Redis lỗi → UNKNOWN không có số liệu; threshold WARN/CRITICAL là heuristic hiển thị, tinh chỉnh theo số liệu thật ở TASK-068D; chưa có số liệu tải thật. **TASK-068D cần đọc:** report 068C mục 8 (bàn giao) — theo dõi `/admin/health` "Queue & worker observability" trong lúc validation; cửa sổ worker metrics là 60 phút gần nhất nên trống nếu worker chưa chạy; chỉnh threshold trong `worker-metrics.ts`/`queue-observability.service.ts` nếu cần, không đổi logic delivery. **Task tiếp theo đề xuất: TASK-068D — 100-mailbox readiness validation.**
- TASK-068D: 100-Mailbox Readiness Validation — done (`npm run verify` PASS: 951 tests, lint/typecheck/build sạch; chờ Gemini review). Validation tổng hợp bằng **synthetic workload** xác nhận hệ thống đủ điều kiện **bắt đầu** test ở mốc ~100 mailboxes; không mailbox/Telegram group khách hàng thật, không gọi Microsoft Graph/Telegram thật, không token/verification code thật. Harness chạy qua đúng production path `processEmailWebhookJob` (worker) → `processGraphMessageJob` (pipeline) với in-memory store/lock/throttle/metrics **thật** (068A/B/C), chỉ mock biên Graph fetch + Telegram send (đếm only). Dataset giả: ~10 customers, 100 ready mailboxes chia sẻ 12 reusable destinations (mỗi mailbox tối đa một active destination), 6 DISABLED + 6 unmapped để test skip an toàn. Chứng minh scenarios A–F: (A) 100 ready → đúng 100 send, không double; (B) webhook+delta cùng graphMessageId → đúng một send (exactly-once); (C) nhiều mailbox chung destination route đúng, không broadcast; (D) DISABLED/unmapped không relay, phân loại skipped không failed; (E) busy-defer chặn bởi maxRetries + throttle wait chặn bởi cap (15s/2s), không retry vô hạn/không drop; (F) observability aggregate chỉ count/ms, snapshot+log không lộ code/email/chatId/token/Redis URL. Chỉ thêm test + docs, không sửa runtime/`.env*`/GitHub Actions/migration; không đổi routing/exactly-once/throughput. File liên quan: `docs/tasks/TASK-068D-100-mailbox-readiness-validation.md`, `docs/reports/TASK-068D-100-mailbox-readiness-validation.md`, `tests/helpers/scale-readiness-fixtures.ts`, `tests/integration/scale-readiness/100-mailbox-readiness.validation.test.ts`. Rủi ro còn lại: đây là synthetic validation, **chưa** phải tải thật — live mailbox/Telegram/code path để internal beta / product trial; throttle clock inject cố định nên số liệu wait minh hoạ cơ chế bị-chặn, không phải benchmark wall-clock; lock in-memory một tiến trình (đa tiến trình dùng Redis lock seam 068A).

## Test phase 2 — Backlog fixes (tiếp)

- TASK-065: Fix legacy Telegram mapping API customer isolation (H1 từ TASK-064) — done (`npm run verify` PASS: 870 tests, lint/typecheck/build sạch; chờ Gemini review). Đã đóng H1: route legacy `POST /api/telegram/mappings` và `PATCH /api/telegram/mappings/[id]` giờ đi qua đường destination-based (đã enforce customer isolation) thay vì nhận raw `telegramChatId`. Mailbox và destination bắt buộc cùng customer; raw chat id không kèm `destinationId` → 400; cross-customer bị chặn kể cả OWNER/ADMIN → 409; STAFF_READ_ONLY vẫn 403. Thêm tham số `scope?` fail-closed (customer scope của người gọi) vào đường destination-based. Giữ nguyên rule nhiều mailbox dùng chung một reusable destination và mỗi mailbox tối đa một active destination; không multi-destination/broadcast; không sửa UI; không sửa `.env*`/GitHub Actions. File liên quan: `docs/tasks/TASK-065-fix-legacy-telegram-mapping-isolation.md`, `docs/reports/TASK-065-fix-legacy-telegram-mapping-isolation.md`, `services/telegram/telegram-mapping.service.ts`, `app/api/telegram/mappings/route.ts`, `app/api/telegram/mappings/[id]/route.ts`, `tests/api/telegram-mappings.route.test.ts`, `tests/unit/telegram/telegram-mapping.service.test.ts`. Rủi ro còn lại: disable/delete by id chưa resolve customer-scope (L2, admin-only, không nhận raw data — follow-up); primitive raw `createTelegramMapping`/`updateTelegramMapping` còn export (không còn route gọi) nên retire ở task dọn dẹp; M1 (dedup race), M2 (TOCTOU one-active), M3 (scope optional), L1 (Telegram `description` log) vẫn là backlog phase 2.

## Pre-live-beta — Multilingual detection readiness

- TASK-069A: Multilingual Facebook/Meta Verification Detection Readiness — implemented, chờ Gemini review (`npm run verify` PASS: 1013 tests, lint/typecheck/build sạch). Mở rộng nhận diện + extract verification code đa ngôn ngữ ở mức tối thiểu, an toàn, **trước** live beta 5–10 mailboxes (trước đó chỉ phủ EN/VI nên email locale khác bị skip im lặng). Kết quả chính: (1) detector và extractor giờ dùng **chung** `services/email/verification-keywords.ts` làm nguồn từ vựng duy nhất → hai tầng không còn lệch logic; (2) thêm hỗ trợ keyword đa ngôn ngữ tối thiểu cho beta (English, Vietnamese, Spanish, Portuguese, French, German, Indonesian, Thai, Chinese giản/phồn thể, Japanese, Korean, Arabic, Russian, Ukrainian), kèm negative keywords (hóa đơn/đơn hàng/điện thoại/IP/ticket…) theo từng locale để không tăng false-positive; (3) mỗi locale có positive + negative tests cho cả detector lẫn extractor; (4) **cố ý giữ cổng code-context** (yêu cầu một "code cue" gần dãy số) thay vì hạ thành "brand gần dãy số" → tránh lấy nhầm số hóa đơn/đơn hàng cạnh tên thương hiệu. Chỉ sửa runtime detector/extractor + thêm module keyword + test + docs; không đổi pipeline/dedup/throttle/queue/routing, không multi-destination/broadcast, giữ rule reusable destination + mỗi mailbox tối đa một active destination, mailbox DISABLED/unmapped không relay; không sửa `.env*`/GitHub Actions; không log/ghi secret/full verification code/full email body (detector không echo code, extractor chỉ trả masked). File liên quan: `docs/tasks/TASK-069A-multilingual-facebook-meta-verification-detection.md`, `docs/reports/TASK-069A-multilingual-facebook-meta-verification-detection.md`, `services/email/verification-keywords.ts`, `tests/unit/email/verification-keywords.multilingual.test.ts`. Rủi ro còn lại: đây vẫn là **synthetic readiness** — cần xác minh email Facebook/Meta thật theo locale của tài khoản người nhận trong live beta. Task tiếp theo: Gemini review TASK-069A → sau PASS mới commit/push → sau đó mới quay lại TASK-069 staged live beta 5–10 mailboxes.
- TASK-069B: Mailbox Token Issue Diagnosis & Reconnect UX — implemented, chờ Gemini review (`npm run verify` PASS: 1024 tests, lint/typecheck/build sạch). Sửa lỗi UX: mailbox ở trạng thái `RECONNECT_REQUIRED` (readiness Token issue) vẫn hiện checklist "✓ Mailbox đã connect" và không có nút reconnect. Nguyên nhân code-level: mailbox detail chỉ phân biệt `DISABLED`, bỏ sót `RECONNECT_REQUIRED` (status này do ba worker đặt khi refresh access bị `invalid_grant`/`interaction_required`). Kết quả chính: (1) checklist readiness tách rõ "mailbox record/provider tồn tại" vs "Microsoft auth/token còn khỏe" vs "active Telegram destination" — không còn tick xanh sai khi mailbox cần reconnect; (2) CTA "Reconnect Hotmail/Outlook" cho cả `DISABLED` và `RECONNECT_REQUIRED`, gate bằng `MANAGE_MAILBOXES`, STAFF_READ_ONLY chỉ thấy hướng dẫn báo OWNER/ADMIN; (3) reconnect dùng lại OAuth connect flow, pin mailbox id mục tiêu → callback fail an toàn (`mailbox_mismatch`) nếu đăng nhập nhầm tài khoản Microsoft khác, không overwrite/duplicate nhầm mailbox; (4) reconnect giữ nguyên customer và Telegram mapping/destination (chỉ cập nhật token + status). Chỉ sửa runtime UI/route/service + test + docs; không đụng worker/pipeline/dedup/throttle/queue/routing, không multilingual/live-beta/scale, giữ rule reusable destination + mỗi mailbox tối đa một active destination; không tạo migration, không sửa `.env*`/GitHub Actions; không log/ghi secret/token/full code/full email body. File: `docs/tasks/TASK-069B-mailbox-token-issue-reconnect-ux.md`, `docs/reports/TASK-069B-mailbox-token-issue-reconnect-ux.md`. Rủi ro còn lại: chưa xác minh live với mailbox Microsoft thật (để internal beta).
- TASK-069C: Harden Refresh Token Failure Classification in Email Worker & Delta Polling — implemented, chờ Gemini review (`npm run verify` PASS: 1049 tests, lint/typecheck/build sạch). Đã làm gì: chuẩn hóa cách ba worker phân loại lỗi refresh token. Trước đây email worker và delta polling gom mọi lỗi (kể cả mạng/timeout/Microsoft 429/5xx) rồi đánh dấu mailbox cần reconnect → mailbox khỏe bị buộc reconnect thủ công vì một blip. Kết quả chính: (1) thêm helper phân loại dùng chung `services/microsoft/refresh-token-failure.ts` làm nguồn sự thật duy nhất (chỉ `invalid_grant`/`interaction_required` mới là cần reconnect; mạng/429/5xx/không rõ là tạm thời); (2) email worker chỉ đánh dấu reconnect khi thật sự cần, lỗi tạm thời trả trạng thái mới `FAILED_TOKEN_TRANSIENT` vẫn retryable nhưng giữ mailbox ACTIVE; (3) delta polling chỉ đánh dấu reconnect khi thật sự cần, lỗi tạm thời vẫn ghi error metadata + đếm failed nhưng giữ ACTIVE để chu kỳ sau retry; (4) subscription renewal chuyển sang dùng cùng helper, giữ nguyên behavior. Chỉ sửa runtime worker/pipeline + helper + test + docs; không đụng UI reconnect (069B), OAuth scope/App Registration, routing/Telegram mapping/reusable destination, queue throughput/concurrency/limiter, multilingual detection; không tạo migration/đổi schema; không sửa `.env*`/GitHub Actions; không log/ghi secret/token/full code/full email body. File: `docs/tasks/TASK-069C-refresh-token-failure-classification-workers.md`, `docs/reports/TASK-069C-refresh-token-failure-classification-workers.md`, `services/microsoft/refresh-token-failure.ts`. Rủi ro còn lại: lỗi giải mã credential vẫn xếp là cần reconnect (đồng nhất renewal cũ) — nếu khóa mã hóa sai diện rộng có thể gắn cờ hàng loạt, là sự cố cấu hình ngoài phạm vi task. Task tiếp theo: Gemini review TASK-069C → sau PASS mới commit/push → quay lại TASK-069 staged live beta.

## Scale-readiness — Distributed pacing (tiếp)

- TASK-070: Redis-backed Global Telegram Bot Pacer — implemented, chờ Gemini review (`npm run verify` PASS: 1060 tests, lint/typecheck/build sạch). Đóng issue **Medium** từ Gemini review tổng quát sau TASK-069C: global Telegram bot pacer (TASK-068B) là **in-memory / per-process** nên khi chạy nhiều worker-email replica, mỗi replica có quota pacing riêng → tổng tốc độ gửi qua một bot có thể vượt ngưỡng ~30 msg/giây của Telegram. **Đã làm gì:** (1) thêm Redis-backed cross-process pacer (`services/queue/redis-global-send-throttle.ts`) reserve qua MỘT Lua script atomic trên một Redis key chung — mọi process serialize trên cùng một slot, mô phỏng đúng logic spacing của destination-throttle (now/interval/cap) nhưng server-side; (2) factory `createGlobalSendThrottle` chọn backend (không có Redis client → in-memory như cũ; có → Redis-backed), mirror `mailbox-lock-factory` (TASK-068A); (3) wire vào `email-worker-runner` gate trên `REDIS_URL` — production tái dùng client ioredis của BullMQ (như TASK-068C, không thêm dependency/không mở socket lúc import, provider lazy), local/test không có `REDIS_URL` giữ in-memory (behavior không đổi); (4) fail-safe: resolve client lỗi/timeout hoặc eval lỗi → degrade về in-process pacing, KHÔNG block delivery/không throw, client resolve một lần + connect timeout 2s. **Kết quả chính:** giữ nguyên interface `GlobalSendThrottle` (reserve giờ có thể async, pipeline `await`), cap thời gian chờ và default pacing an toàn của TASK-068B (40ms/slot ≤ ~25 send/giây, cap 2s); giữ exactly-once (TASK-068A), routing/reusable destinations + rule mỗi mailbox tối đa một active destination, không broadcast/không multi-destination/không đổi số lần gửi. Không thêm env var mới; không log/ghi token/secret/bot token/database-Redis URL/encryption key/session secret/full code/full email body (log lỗi Redis chỉ message tĩnh + context rỗng, có test khẳng định không lộ `redis://`/host/pass). Chỉ sửa runtime + test + docs; không sửa `.env*`/GitHub Actions/package scripts/migration. File liên quan: `docs/tasks/TASK-070-redis-backed-global-telegram-bot-pacer.md`, `docs/reports/TASK-070-redis-backed-global-telegram-bot-pacer.md`, `services/queue/redis-global-send-throttle.ts`, `services/queue/global-send-throttle-factory.ts`. Rủi ro còn lại: (a) clock skew giữa replica → pacing là smoother xấp xỉ, không hard guarantee (có thể chuyển `redis TIME` sau); (b) Redis lỗi → degrade về per-process pacing tới khi Redis hồi phục; (c) **chưa chạy tải thật** — số nhịp thật cần xác minh ở controlled live beta. **Task tiếp theo đề xuất: controlled live beta / test-mailbox trial** (quay lại TASK-069 staged live beta 5–10 mailboxes với email/Telegram thật).
- TASK-071: Delta polling 403 classification and reconnect loop hardening — implemented, chờ Gemini review (`npm run verify` PASS: 1065 tests, lint/typecheck/build sạch). Đóng sự cố live beta: mailbox có **refresh token vẫn khỏe** (rotate thành công mỗi cycle) nhưng Graph **delta data request trả HTTP 403** bị delta polling kết án thành `RECONNECT_REQUIRED` → reconnect xong vài phút lại rơi lại (vòng lặp). Điều tra log staging (chỉ log đã sanitize, không probe Graph/không redeem token/không chạm DB): mẫu lặp `Persisted rotated refresh token` → ~2–4s sau `DeltaPollingHttpError`; toàn fleet 24h ~14 lỗi delta gần như **đều của một mailbox** → lỗi riêng mailbox, không app-wide, không phải lỗi token. **Đã làm gì:** (1) `delta-polling.service.ts` tách `401 → 'auth'` (giữ reconnect) và **`403 → 'forbidden'` (KHÔNG reconnect)**; (2) self-heal cursor an toàn — 403 khi mailbox **đang có** `microsoftDeltaCursor` → `resetDeltaCursor` (cycle sau bootstrap lại; cơ chế vòng lặp là reconnect không reset cursor nên cursor độc bị tái dùng), 403 lúc bootstrap (cursor null) → không reset, chỉ retry; (3) sanitized Graph diagnostics — trích `error.code`/`innerError.code`/header `request-id` vào `deltaLastErrorMessage` (`code=… inner=… reqId=…`), **không** đọc `error.message`/token; (4) đồng bộ worker — `subscription-renewal.service.ts` `classifyRenewError` đổi `permission` (403) → `transient` (khớp delta + email; email vốn đã không mark trên 403). Giữ nguyên đường refresh token thật (`invalid_grant`/`interaction_required` vẫn reconnect ngay), exactly-once, routing/reusable destination + mỗi mailbox tối đa một active destination; không tạo migration/đổi schema (`microsoftDeltaCursor` đã có), không đụng OAuth/reconnect UX (069B), dedup/throttle/queue, `.env*`/GitHub Actions; không log/ghi secret/token/full code/full email body. File: `docs/tasks/TASK-071-delta-polling-403-reconnect-loop-hardening.md`, `docs/reports/TASK-071-delta-polling-403-reconnect-loop-hardening.md`, `services/microsoft/delta-polling.service.ts`, `services/queue/workers/delta-polling-runner.ts`, `services/microsoft/subscription-renewal.service.ts`. Rủi ro/đánh đổi còn lại: reset cursor khi 403-có-cursor tạo khoảng trống nhỏ (bootstrap mới không enqueue mail cũ) — chấp nhận được vì webhook là đường primary và cursor đang 403 vốn không relay được gì; 403 account/endpoint-level thật sẽ tự reset→bootstrap→vẫn 403 và được ghi lại với diagnostics để ops xử lý; **chưa xác minh live** (theo yêu cầu không chạm production) — `error.code` thật sẽ xuất hiện trong `deltaLastErrorMessage` ở lần fail kế tiếp nhờ logging mới.
- TASK-072: Full project logic, security and live-beta readiness audit — **đã hoàn tất**, dạng audit-only (chỉ tạo task file và audit report, **không** sửa runtime code, không sửa test, schema, migration hay CI). Gemini review độc lập đã PASS và baseline verification đã PASS. **Kết quả chính:** Critical **0**; High **1**; còn lại các vấn đề mức Medium và Low đưa vào backlog thành những task fix riêng. High chính: khi webhook xử lý notification mà bước enqueue thất bại nhưng route vẫn trả mã 202 thì Microsoft không gửi lại, dẫn tới rủi ro **bỏ sót verification code** (chỉ có delta polling backup cứu, nhạy về thời gian). File liên quan: `docs/tasks/TASK-072-full-project-logic-security-live-beta-readiness-audit.md` và `docs/reports/TASK-072-full-project-logic-security-live-beta-readiness-audit.md`. Rủi ro còn lại: cần xử lý webhook enqueue failure **trước khi scale live beta**; các vấn đề Medium (phân loại lỗi quyền ở email pipeline, RBAC scope cho phần mapping và destination, lớp dedup phụ, guard concurrency cho renewal, dọn metadata lỗi delta sau self-heal) sẽ làm theo task riêng. **Task tiếp theo đề xuất: TASK-073 — webhook enqueue failure handling** (cho route trả mã không phải 2xx khi enqueue thất bại để Microsoft retry; jobId deterministic đảm bảo retry không tạo trùng).
- TASK-073: Webhook enqueue failure handling — **đã hoàn tất**, Gemini review đã PASS. Fix High finding của TASK-072. **Kết quả chính:** webhook route trả mã 503 khi có enqueue thất bại thật sự, kể cả khi chỉ một phần batch hỏng; trường hợp validationToken vẫn trả 200 text/plain và không phụ thuộc queue; notification sai clientState vẫn bị skip an toàn và không bị coi là enqueue failure; không thêm retry loop trong route (mỗi notification chỉ enqueue một lần). Việc Microsoft retry cả batch an toàn nhờ jobId deterministic và lớp dedup hiện có nên không gửi trùng. Thay đổi runtime tối thiểu ở route, thêm test cho nhánh enqueue failure; baseline verification đã PASS. File liên quan: `docs/tasks/TASK-073-webhook-enqueue-failure-handling.md` và `docs/reports/TASK-073-webhook-enqueue-failure-handling.md`. Rủi ro còn lại: chưa kiểm chứng hành vi retry thực của Microsoft Graph bằng live beta; phần rate-limit và kiểm tra nguồn cho endpoint vẫn để task sau. **Task tiếp theo đề xuất: TASK-074 — đồng bộ phân loại Graph 403 trong email pipeline với TASK-071** (hoặc task kế tiếp theo thứ tự ROADMAP hiện hành nếu đã đổi).
- TASK-074: Sync email pipeline Graph 403 classification with TASK-071 — **đã hoàn tất** (Gemini review PASS, `npm run verify` PASS: 1074 tests). Đồng bộ cách xử lý Graph 403 ở email pipeline cho khớp với TASK-071. **Kết quả chính:** email pipeline không còn quy Graph 403/lỗi phân quyền thành reconnect-required; 403 rơi về một status trung tính (non-reconnect) thay vì buộc reconnect; 403 không còn flip mailbox sang `RECONNECT_REQUIRED`; 401/lỗi auth vẫn giữ reconnect-required như trước; toàn bộ hành vi 403 giờ thống nhất với delta polling (TASK-071). Giữ nguyên refresh-token failure classification (TASK-069C), dedup exactly-once, webhook enqueue failure handling (TASK-073), và delta polling backup; không đổi routing/reusable destinations + rule mỗi mailbox tối đa một active destination, không broadcast/multi-destination; không log/ghi secret/token/full code/full email body. File liên quan: `docs/tasks/TASK-074-sync-email-pipeline-graph-403-classification.md` và `docs/reports/TASK-074-sync-email-pipeline-graph-403-classification.md`. Rủi ro còn lại: trường hợp 403 lặp lại ở mức account/endpoint vẫn cần backoff/alert riêng (để TASK-075); chưa kiểm chứng live với Microsoft Graph thật. **Task tiếp theo đề xuất: TASK-075 — delta persistent 403 backoff/alert** (hoặc tên tương đương theo thứ tự ROADMAP hiện hành).
- TASK-075: Delta persistent 403 backoff/alert — **đã hoàn tất** (Gemini review PASS, `npm run verify` PASS: 1081 tests). Đóng finding Medium còn lại của TASK-072: delta polling khi gặp Graph 403 mức account/endpoint lặp lại (sau self-heal cursor TASK-071 vẫn 403 mỗi cycle) trước đây retry full-speed vô hạn, không backoff, không escalation. **Kết quả chính:** (1) thêm persistent backoff/cooldown theo mailbox, lưu DB nên sống qua worker restart; (2) mailbox đang trong cooldown bị skip an toàn — không fetch token, không gọi Graph, không tính là failure của cycle; (3) 403 account/endpoint-level lặp lại **không** flip mailbox sang `RECONNECT_REQUIRED`; (4) 401/lỗi auth vẫn giữ reconnect-required như trước; (5) khi 403 vượt ngưỡng có alert/health signal an toàn cho OWNER/ADMIN (qua kênh admin sẵn có, chỉ trường đã mask, không lộ thông tin nhạy cảm); (6) poll thành công sẽ clear forbidden backoff state và dọn stale delta error metadata (gộp luôn finding self-heal cleanup). Giữ nguyên self-heal TASK-071, đồng bộ 403 ở email pipeline (TASK-074), refresh-token failure classification (TASK-069C), dedup exactly-once, routing/reusable destinations + rule mỗi mailbox tối đa một active destination; không đụng webhook (TASK-073), RBAC/customer-scope, dedup receivedAtBucket, subscription renewal concurrency, webhook rate-limit, UI; không sửa `.env*`/GitHub Actions; không log/ghi thông tin nhạy cảm/full code/full email body. File liên quan: `docs/tasks/TASK-075-delta-persistent-403-backoff-alert.md` và `docs/reports/TASK-075-delta-persistent-403-backoff-alert.md`. Rủi ro còn lại: chưa xác minh live với Microsoft Graph thật; ngưỡng/cooldown có thể cần tinh chỉnh theo số liệu live beta. **Task tiếp theo đề xuất: TASK-076 — RBAC/customer-scope wiring cho mapping/destination** (finding Medium của TASK-072; chỉ ghi định hướng, ngoài scope TASK-075) hoặc task kế tiếp theo thứ tự ROADMAP hiện hành.

## Tooling — Reviewer migration

- TASK-076: Antigravity CLI reviewer migration — **đã hoàn tất** (docs/workflow-only; không sửa runtime code/test/schema/migration/GitHub Actions). Chuyển vai trò reviewer/tester độc lập của project từ **Gemini CLI** sang **Antigravity CLI** theo việc Google chuyển terminal workflow sang Antigravity CLI. **Kết quả chính đã chốt:** (1) `git mv GEMINI.md ANTIGRAVITY.md` + viết lại nội dung thành instruction mới cho Antigravity CLI (mặc định chỉ review/test, chỉ sửa khi prompt có `ALLOW_ANTIGRAVITY_EDIT=true`, luôn kết luận PASS/FAIL, không đọc/in `.env*`, không log token/refresh token/client secret/bot token/full verification code/full email body); (2) `git mv scripts/gemini-ecc-review.ps1 scripts/antigravity-ecc-review.ps1` + cập nhật prompt/lệnh gọi/output; (3) cập nhật source-of-truth hiện hành: `AGENTS.md`, `docs/SECURITY_RULES.md`, `CLAUDE.md`, `.cursor/rules/ecc-project-rules.mdc`, `docs/PRODUCT_SPEC.md` (tiêu chí nghiệm thu #7), `docs/operations/PRODUCTION_SCALE_UP_CHECKLIST.md`, `docs/MICROSOFT_SETUP.md`. Các vai trò khác giữ nguyên (ChatGPT planner/PM, Claude Code coder chính, Cursor xem/sửa nhỏ, GitHub Actions CI gate). File liên quan: `docs/tasks/TASK-076-antigravity-cli-reviewer-migration.md`, `docs/reports/TASK-076-antigravity-cli-reviewer-migration.md`, `ANTIGRAVITY.md`, `scripts/antigravity-ecc-review.ps1`. **Rủi ro còn lại:** mention "Gemini" còn lại trong các report/handoff/task CŨ là **lịch sử thật**, cố ý giữ nguyên (không viết lại review verdict của task đã được Gemini review). **Task tiếp theo:** dùng Antigravity CLI làm reviewer cho các task sắp tới; quay lại finding RBAC/customer-scope wiring cho mapping/destination (Medium của TASK-072) hoặc task kế tiếp theo thứ tự ROADMAP hiện hành.

## Post-Antigravity — Audit & RBAC hardening

- TASK-077: Post-Antigravity full project logic, security & readiness audit — **đã hoàn tất** (audit-only; **không** sửa runtime code/test/schema/migration/CI/package scripts; chỉ tạo task + report). `npm run verify` PASS (exit 0, 91 test files / 1081 tests) theo report; **Antigravity CLI review đang chờ** (report chưa ghi nhận PASS). Vòng audit tổng thể sau migration sang Antigravity (TASK-076), xác minh lại trạng thái code hiện tại của các finding TASK-072 và đối chiếu những gì TASK-073/074/075 đã đóng. **Kết quả chính:** Critical **0**; High mới **0** (High của TASK-072 đã đóng ở TASK-073, xác minh fix có trong code); **4 Medium còn mở** — (M1) RBAC/customer-scope chưa wire đủ ở đường ghi Telegram mapping/destination (latent, hiện chỉ OWNER/ADMIN chạm tới), (M2) subscription renewal thiếu guard concurrency (rủi ro đa-replica double-PATCH), (M3) webhook endpoint chưa rate-limit/verify origin (DoS surface), (M4) thiếu test cross-customer cho create/update/disable/delete; cùng một số Low (Redis pacer latch fail-open, UI badges, renewal window hardcode, audit numeric-code guard). **Đính chính TASK-072:** lớp dedup code+time-bucket (`receivedAtBucket`) **không** phải no-op trong production (Prisma store match theo `codeHash`+`receivedAt` window) → bỏ đề xuất thêm cột; chỉ còn Low (thiếu DB constraint cho lớp này). **Đã xác nhận ổn:** exactly-once, customer isolation đường relay chính, encryption-at-rest, secret hygiene/masking, reconnect 401-vs-403 (071/074/075), refresh-token classification (069C), và Antigravity migration đúng/sạch. File liên quan: `docs/tasks/TASK-077-post-antigravity-full-project-audit.md`, `docs/reports/TASK-077-post-antigravity-full-project-audit.md`. Rủi ro còn lại: nhóm Medium cần đóng trước khi (a) cấp quyền ghi cho role staff giới hạn scope, (b) chạy nhiều replica renewal, (c) mở rộng bề mặt webhook public; nhiều finding mới xác minh ở mức đọc code (chưa chạy production). **Task tiếp theo đề xuất: TASK-078 — RBAC/customer-scope hardening cho Telegram mapping/destination (M1/M4).**
- TASK-078: RBAC / customer-scope hardening cho Telegram mapping & destination operations — **đã hoàn tất** theo report; `npm run verify` PASS (exit 0, 91 test files / **1098 tests**, +17 test mới) theo report; **Antigravity CLI review đang chờ** (report chưa ghi nhận PASS). Đóng finding Medium **M1/M4** của TASK-077: customer-scope giờ enforce **fail-closed** trên toàn bộ mapping/destination operations qua UI, API, và server action. **Kết quả chính:** (1) mọi caller (edit page mapping/destination, API `?action=disable` + `DELETE`, 4 server action mapping + 3 destination action) resolve scope của user hiện tại và truyền vào service; (2) thêm `scope?` + guard fail-closed cho `disableTelegramMapping`/`deleteTelegramMapping` và `createTelegramDestination`/`updateTelegramDestination`/`disableTelegramDestination`; thêm `TelegramScopeError` dùng chung, API map về **404** (không lộ tồn tại); (3) scope `all` (OWNER/ADMIN) là no-op không tốn thêm query — flow hợp lệ giữ nguyên; worker/system bỏ scope vẫn unscoped. STAFF_READ_ONLY vẫn bị chặn ở permission (403); user scope-limited không thể read/edit/update/disable/delete resource ngoài customer được phép; cross-customer mailbox/destination vẫn bị chặn (giữ TASK-065). Giữ nguyên rule nhiều mailbox dùng chung reusable destination nhưng mỗi mailbox tối đa một active destination; **không** multi-destination/broadcast, **không** UI redesign, **không** live beta thật, **không** đổi queue/dedup/delta/renewal/webhook, **không** sửa schema/migration/`.env*`/GitHub Actions/package scripts. File liên quan: `docs/tasks/TASK-078-rbac-customer-scope-telegram-operations.md`, `docs/reports/TASK-078-rbac-customer-scope-telegram-operations.md`, `services/telegram/telegram-scope-error.ts`, `services/telegram/telegram-mapping.service.ts`, `services/telegram/telegram-destination.service.ts`, `services/telegram/mapping-actions.ts`, `services/telegram/destination-actions.ts`, `app/api/telegram/mappings/[id]/route.ts`, `app/admin/telegram/[id]/edit/page.tsx`, `app/admin/telegram/destinations/[id]/edit/page.tsx`. Rủi ro còn lại: dropdown `listCustomers()` ở trang edit destination chưa scope (defense-in-depth, **không** phải lỗ ghi vì action vẫn fail-closed; chỉ rò tên customer cho role giới hạn scope — hiện chỉ OWNER/ADMIN chạm tới) → để task dọn dẹp. **Task tiếp theo:** một incident production xảy ra trước khi làm M2/M3, nên TASK-079 được dùng cho chẩn đoán incident (xem mục dưới); M2 (renewal concurrency) và M3 (webhook rate-limit/origin) được dời số về sau.

## Incident response — Relay silence

- TASK-079: Relay Silence Incident Diagnosis — **COMPLETED, ROOT CAUSE CONFIRMED** (investigation-only; **không** sửa runtime/test/schema/migration/`.env*`/GitHub Actions; không commit/push; chỉ tạo task + report). `npm run verify` PASS (exit 0, 91 test files / 1098 tests, lint/typecheck/build sạch). **Incident:** ~3 tuần (từ ~cuối tháng 7) không có verification code nào relay tới Telegram; Code Event Logs không có sự kiện mới; mailbox cũ vẫn "connected", mailbox mới connect cũng câm. **Root cause vận hành trực tiếp (đã xác nhận bằng runtime evidence + recovery):** delta polling runner bị **stuck trong một poll cycle** — tiến trình `worker-delta` vẫn active nhưng một cycle không bao giờ kết thúc nên scheduler coi "previous tick still running" và **skip mọi tick sau** (log lặp `skipping tick`), polling ngừng enqueue fleet-wide. **Recovery:** human operator **restart riêng `worker-delta`** (không đổi variables) → delta poll trở lại → Graph jobs enqueue lại → `worker-email` xử lý lại → Telegram nhận verification messages trở lại. **Đã loại trừ (có bằng chứng):** không phải regression TASK-078 (RBAC không chạm runtime resolution của worker, chưa deploy); không phải detector/extractor (nếu detector từ chối vẫn ghi `DETECTOR_REJECTED` — log không thể trống); không phải Telegram routing; `worker-email` không phải root cause chính (vẫn tiêu thụ job khi có job); Redis queue mismatch không được runtime evidence hỗ trợ (cấu hình đồng nhất, recovery không đổi variable). **Cơ chế code (đọc, không sửa):** `startDeltaPollingScheduler` chỉ clear cờ `inflight` trong `finally`, còn `runDeltaPollingOnce` gọi Graph/token qua native `fetch` **không có timeout/AbortController` → một request treo khoá scheduler vô hạn. File liên quan: `docs/tasks/TASK-079-relay-silence-incident-diagnosis.md`, `docs/reports/TASK-079-relay-silence-incident-diagnosis.md`. **Remaining risks / handoff:** (1) delta polling cần timeout/self-recovery để một cycle không block scheduler vô hạn; (2) sau downtime dài, backlog chứa message cũ → rủi ro relay **stale verification message**; (3) production path chưa có Graph subscription wiring thực tế cho mailbox mới (renewal không có candidate) — finding kiến trúc riêng. **Task tiếp theo đề xuất: TASK-080 — Delta stuck-cycle recovery + stale message protection** (thêm timeout/watchdog cho poll cycle + drop message quá tuổi); sau đó **task riêng cho Graph subscription wiring**; M2 (renewal concurrency) và M3 (webhook rate-limit/origin) của TASK-077 vẫn trong backlog.
- TASK-080: Delta Polling Stuck Recovery & Stale Message Relay Protection — **implementation hoàn tất; Antigravity CLI review PASS** (không có finding Critical/High/Medium; chỉ Low đã chấp nhận trong scope — xem dưới). `npm run verify` PASS (exit 0, **96 test files / 1115 tests**, lint/typecheck/build sạch; Antigravity chạy độc lập cũng PASS). **Root cause kế thừa TASK-079:** delta polling có thể bị stuck khi một Microsoft HTTP operation không settle, khiến scheduler giữ `inflight` và skip tick vô hạn. **Fix chính:** (A) delta stuck recovery — finite timeout + **cancellation thật bằng AbortController** cho delta Graph request và cho refresh-token HTTP operation trên delta path; giữ non-overlap scheduler và chỉ release `inflight` **sau khi** operation settle/cancel (không watchdog reset khi op cũ còn chạy); timeout được classify **transient**, **không** phá 403/reconnect của TASK-071/TASK-075 (real 401/403 giữ nguyên); (B) stale verification guard trong graph message pipeline dùng Microsoft `receivedDateTime` (không dùng enqueue/processing time), **maximum relay age hiện là 30 phút**, message quá tuổi là **terminal skip** — không Telegram send, không BullMQ failure/retry, ghi CodeEvent skip an toàn (không code/email body); dedup/exactly-once giữ nguyên (guard đặt sau early-dedup, trước detector/Telegram). **Low finding đã chấp nhận (không sửa trong TASK-080):** delta HTTP timeout là code-level constant 20 giây và stale threshold là code-level constant 30 phút — nếu cần configurable thì mở task riêng. **Remaining risks:** timeout 20s + stale 30m là code-level constants; theo report TASK-080, timeout cho Graph mail/email-worker HTTP path và OAuth token-exchange nằm **ngoài scope** TASK-080 (không phải root cause TASK-079). **Deferred (task riêng tiếp theo):** Graph subscription creation/reconciliation/wiring **KHÔNG** thuộc TASK-080. File liên quan: `docs/tasks/TASK-080-delta-polling-stuck-recovery-stale-message-protection.md`, `docs/reports/TASK-080-delta-polling-stuck-recovery-stale-message-protection.md`. **Task tiếp theo cần đọc TASK-079 + TASK-080** để hiểu: (1) vì sao delta polling hiện là **critical path** (production chưa wire Graph subscription nên webhook path không tồn tại cho mailbox mới → delta polling là đường sống); (2) finding **Graph subscription wiring đã được tách riêng** thành task kế tiếp.

- TASK-081: Graph Subscription Connect-Time Provisioning — **implementation hoàn tất; Antigravity CLI review PASS** (Critical 0 / High 0 / Medium 0; một Low non-blocking: outer try/catch ở OAuth callback là defensive/redundant nhưng an toàn, không sửa trong task này). `npm run verify` PASS (exit 0, **98 test files / 1142 tests**, lint/typecheck/build sạch). **Scope đã khóa: Option A only — connect-time provisioning.** Đóng missing production caller gap mà TASK-079 xác nhận (finding F1): trước đây không có production code path nào gọi `createInboxSubscription`, mailbox mới connect không bao giờ có Graph subscription. **Kết quả chính:** (1) OAuth connect/reconnect sau khi `saveConnectedMailbox` thành công giờ chạy connect-time ensure/provisioning (`ensureInboxSubscriptionForConnectedMailbox`) với fresh access token từ token exchange (không persist token mới); (2) **ensure semantics, không blind-create:** subscription local còn khả năng sống (ACTIVE/RENEWING/FAILED và chưa hết hạn) → no-op, không duplicate; không có usable subscription hoặc subscription đã hết hạn rõ ràng/EXPIRED → provision mới; (3) **fail-open:** provisioning lỗi (config/4xx/5xx/network/timeout/DB) không rollback mailbox/credential, không flip `RECONNECT_REQUIRED`, connect vẫn success, delta polling backup vẫn hoạt động, không retry loop; (4) connect-time Graph create có **finite timeout 20s + real cancellation** (reuse `fetch-with-timeout` của TASK-080, không đổi timeout behavior của renewal/delete paths); (5) Microsoft remote create thành công nhưng DB persist fail → **best-effort compensating remote DELETE đúng một lần** rồi dừng, không local ACTIVE row giả; (6) clientState plaintext→hash semantics giữ nguyên (chỉ persist `clientStateHash`); (7) subscription mới tương thích renewal worker hiện hành; renewal/disconnect (TASK-052)/exactly-once behavior giữ nguyên, có regression test. **KHÔNG có trong TASK-081:** automatic reconciliation, production backfill, fleet scan, renewal redesign, schema/migration. File liên quan: `docs/tasks/TASK-081-graph-subscription-connect-time-provisioning.md`, `docs/reports/TASK-081-graph-subscription-connect-time-provisioning.md`. **Remaining risks / deferred:** (1) existing ACTIVE mailboxes đã thiếu Graph subscription chưa được tự động reconcile/backfill; (2) concurrent duplicate OAuth callbacks vẫn có cửa sổ nhỏ có thể tạo duplicate remote subscription — distributed locking/constraint deferred; (3) renewal concurrency finding cũ vẫn deferred; (4) chưa chạy live Microsoft validation cho TASK-081; (5) connect provisioning timeout là code-level constant 20s. **Candidate follow-up: Graph subscription reconciliation for existing ACTIVE mailboxes** (chưa được duyệt thành task bắt buộc).

- TASK-082: Graph Subscription Reconciliation — **implementation hoàn tất; Antigravity CLI review PASS**. `npm run verify` PASS (exit 0, **101 test files / 1185 tests**, lint/typecheck/build sạch). Đóng phần deferred của TASK-081: thêm **reconciliation capability** cho các Microsoft mailbox `ACTIVE` hiện hữu có credential nhưng thiếu potentially-live Graph subscription (connect trước TASK-081, chỉ sống nhờ delta polling backup). **Kết quả chính:** (1) capability chỉ **operator-invoked one-shot** (CLI `npm run reconcile:subscriptions`) — không periodic scheduler, không reconciliation pass trong renewal worker, không auto-backfill khi deploy/startup; (2) mặc định **dry-run** thật sự non-mutating (không token refresh, không Graph call, không DB write); chỉ explicit `--apply` mới mutate; (3) bounded mailbox count mỗi invocation (default 5, hard maximum 20 là hằng code-level) và xử lý **sequential** (concurrency 1, không có option concurrency); (4) reuse nguyên trạng TASK-081 potentially-live/ensure provisioning semantics (dùng chung định nghĩa blocking subscription, không blind-create) và reuse existing encrypted refresh-token/access-token path — rotated refresh credential vẫn encrypted-at-rest, access token chỉ in-memory, TASK-069C classification nguyên vẹn; (5) token-refresh path của reconciliation có **finite HTTP timeout + AbortController cancellation thật** (đóng finding High của Antigravity review lần 1); timeout classify **transient** — không flip mailbox sang reconnect sai, không retry vô hạn, batch tiếp tục với mailbox độc lập kế tiếp; (6) **disconnect race** xử lý fail-safe: re-check mailbox còn ACTIVE sau token refresh và sau provisioning; nếu mailbox rời ACTIVE sau remote create thì local subscription mới bị đưa về trạng thái non-usable **trước** khi best-effort remote cleanup đúng một lần; reconnect-marking là conditional nên không overwrite trạng thái disconnect; (7) **không** schema/migration, **không** worker mới, **không** env reconciliation flag, **không** distributed lock; **không chạy production reconciliation trong TASK-082** — chạy thật là ops action riêng (dry-run xem kết quả trước, rồi mới apply). File liên quan: `docs/tasks/TASK-082-graph-subscription-reconciliation.md`, `docs/reports/TASK-082-graph-subscription-reconciliation.md`. **Deferred risks:** (1) race hẹp giữa TASK-081 connect-time provisioning và TASK-082 reconciliation vẫn là Deferred Risk ở v1 vì scope không thêm distributed lock/schema constraint (hệ quả bounded bởi exactly-once dedup và disconnect cleanup hiện có); (2) `SUBSCRIPTION_EXPIRED` lifecycle relay gap vẫn là **Deferred Finding riêng**, không nằm trong reconciliation candidates của TASK-082. **Task tiếp theo cần human/ChatGPT duyệt scope và gán số** — không tự tạo task mới trong ROADMAP này.

- TASK-083: SUBSCRIPTION_EXPIRED Mailbox Recovery & Relay Continuity — **completed; Antigravity CLI re-review cuối PASS**. `npm run verify` PASS (exit 0, **102 test files / 1209 tests**, lint + typecheck + build sạch). **Investigation đã confirm relay blind spot:** mailbox `SUBSCRIPTION_EXPIRED` bị loại khỏi delta polling (query chỉ poll ACTIVE), subscription local không còn usable cho webhook, và pipeline gate chỉ relay mailbox ACTIVE → mailbox câm vô hạn. **Root cause:** renewal lifecycle flip mailbox sang `SUBSCRIPTION_EXPIRED` (subscription hết hạn local hoặc Graph 404/410) trong khi refresh credential thường vẫn còn hợp lệ, và không có code path/CTA nào đưa mailbox trở lại ACTIVE. **Kết quả chính:** (1) operator-invoked recovery trên reconciliation capability TASK-082 — recovery mode là **opt-in** (flag CLI riêng; không flag → behavior TASK-082 nguyên vẹn); (2) dry-run là mặc định, chỉ explicit apply mới mutation; bounded batch + sequential reuse TASK-082; (3) candidate recovery chỉ dành cho `SUBSCRIPTION_EXPIRED` qua query riêng — KHÔNG nới normal ACTIVE query thành status union; (4) reuse nguyên trạng TASK-081 subscription ensure, TASK-082 token/reconciliation seams (finite timeout + cancellation), TASK-069C classification, và cleanup seam hiện có; (5) mailbox chỉ chuyển `SUBSCRIPTION_EXPIRED → ACTIVE` SAU khi usable Graph subscription đã được tạo và persist thành công, qua **conditional transition** không bao giờ overwrite `DISABLED` hoặc `RECONNECT_REQUIRED`; (6) **High finding từ Antigravity review đầu** (concurrent OAuth reconnect race: recovery có thể cleanup subscription đang được reconnect sử dụng — mailbox ACTIVE mất webhook path) đã được fix hẹp: conditional flip fail → re-read mailbox state; `ACTIVE` → GIỮ usable subscription; `DISABLED`/`RECONNECT_REQUIRED`/unexpected non-ready state → cleanup fail-safe đúng một lần; (7) **không** schema/migration, distributed lock, Redis change, worker/scheduler mới, UI change, hay `.env*` change. File liên quan: `docs/tasks/TASK-083-subscription-expired-recovery.md`, `docs/reports/TASK-083-subscription-expired-recovery.md`. **Remaining risks:** (1) cửa sổ TOCTOU có thể tạo duplicate remote subscription khi recovery/reconnect create đồng thời — v1 chấp nhận bounded risk, downstream exactly-once ngăn double relay; (2) edge inconsistent state (`SUBSCRIPTION_EXPIRED` nhưng còn blocking/live row) vẫn deferred, không auto-repair; (3) chưa chạy recovery trên production/live Microsoft mailbox. **Task tiếp theo sẽ được chọn sau khi TASK-083 completion/commit/push hoàn tất** — cần human/ChatGPT duyệt.

- TASK-084: Subscription Renewal Concurrency Guard — **implementation hoàn tất; Antigravity CLI Final Implementation Review PASS**. `npm run verify` PASS (exit 0, **102 test files / 1239 tests**, lint + typecheck + build sạch; Antigravity xác minh độc lập, `git diff --check` sạch). **Nguồn gốc:** finding M2 của audit TASK-077 (renewal thiếu guard concurrency) + single-replica assumption của TASK-082/083 không được enforce ở code — hai tiến trình renewal (multi-replica, scheduler + one-shot thủ công, hay rolling-restart overlap) có thể cùng renew một subscription và ghi đè trạng thái của nhau. **Kết quả chính:** (1) **atomic DB claim** cho mỗi subscription TRƯỚC token/PATCH — `updateMany` có điều kiện (ACTIVE/FAILED, hoặc RENEWING đã stale) → chỉ một winner (affected count = 1), loser bỏ qua không side-effect; (2) **claim-generation CAS** trên cột `GraphSubscription.updatedAt` có sẵn (không schema): worker **tự mint `claimTimestamp` và ghi tường minh** vào `updatedAt`, giữ làm token local, read-back **chỉ để verify** round-trip (fail-closed nếu lệch) — KHÔNG adopt giá trị đọc lại (đóng finding HIGH của ChatGPT technical review về hijack generation của stale-reclaimer); mọi completion đòi `status=RENEWING AND updatedAt=claimTimestamp`; (3) **stale RENEWING reclaim** sau **30 phút** (hằng code-level, không env); (4) **stale claimant** mất ownership → completion count = 0 → KHÔNG ghi đè subscription và KHÔNG mailbox lifecycle side-effect (correction A: mọi mailbox write chỉ sau khi CAS chứng minh còn giữ claim; CAS count 0 / throw → zero mailbox effect); (5) **TASK-083 replacement guard:** mailbox chỉ chuyển ACTIVE → SUBSCRIPTION_EXPIRED khi KHÔNG còn GraphSubscription khác possibly-live (relation-predicate reuse đúng blocking definition TASK-081/082, loại chính row đang lỗi) — subscription thay thế còn sống thì mailbox giữ ACTIVE; (6) **disconnect** không bị late renewal resurrect/overwrite (write của disconnect bump `updatedAt` làm CAS cũ vô hiệu; writer đòi ACTIVE / status≠DISABLED nên DISABLED không bị đè); (7) **OAuth reconnect guard (correction B):** RECONNECT_REQUIRED chỉ ghi khi mailbox credential generation (opaque marker, không log) chưa đổi — Case A (reconnect trước rotation) và Case B (Graph 401 sau khi lấy token, dùng generation post-rotation) — nên stale renewal không kéo mailbox vừa OAuth-reconnect về RECONNECT_REQUIRED; (8) **genuine TASK-069C behavior giữ nguyên** (invalid_grant / interaction_required / 401 vẫn reconnect khi credential không đổi; 403 vẫn transient theo TASK-071); `renewGraphSubscription` thu về thin Graph PATCH adapter (persistence chuyển hẳn về repo layer có CAS); observability thêm `claimLostCount` / `staleReclaimedCount`. **Không** schema/migration, version column, Redis/distributed lock, worker/scheduler/queue mới, env/UI/CI change. File liên quan: `docs/tasks/TASK-084-subscription-renewal-concurrency-guard.md`, `docs/reports/TASK-084-subscription-renewal-concurrency-guard.md`. **Remaining / deferred risk:** broader multi-worker **credential-rotation last-writer-wins race** (renewal / delta polling / email worker / reconciliation cùng ghi refresh credential không version) vẫn **DEFERRED** sang follow-up riêng — TASK-084 **KHÔNG** sửa race này; residual bounded của reconnect guard (operation tự rotate đồng thời OAuth reconnect dưới LWW) là cùng deferred item, không phải blocker mới.

- TASK-085: Multi-Worker Credential Rotation Concurrency Guard — **implementation hoàn tất; Antigravity CLI Final Implementation Review PASS** (không có finding mới; finding Medium về `DISABLED` từ Architecture Review đã được implement). `npm run verify` PASS (exit 0, **102 test files / 1244 tests**, lint + typecheck + build sạch; Antigravity xác minh độc lập, `git diff --check` sạch). **Nguồn gốc:** đóng đúng deferred item mà TASK-084 (A6/D4) đã confirm — "broader multi-worker credential-rotation last-writer-wins race": renewal / delta polling / email worker / reconciliation cùng ghi refresh credential không version. **Kết quả chính:** (1) shared credential rotation persistence chuyển từ **last-writer-wins sang conditional DB CAS** (`mailbox.update` → `mailbox.updateMany` có điều kiện), không schema/migration; (2) **ownership guard dùng exact old `Mailbox.encryptedRefreshToken` làm opaque generation marker** (không log, không so plaintext, không dùng `updatedAt`/`tokenLastRefreshedAt` làm marker, không re-read DB để adopt generation khác); (3) **CAS predicate gồm ba điều kiện**: mailbox id, `status` khác `DISABLED`, và `encryptedRefreshToken` khớp expected generation; (4) **affected count 0 là controlled CAS loss** — discard rotated credential, KHÔNG overwrite DB, KHÔNG `reconnect_required`, KHÔNG blind retry; access token của cycle hiện tại vẫn dùng bình thường, cycle sau tự đọc generation mới; (5) **DB infrastructure throw được propagate bằng sanitized error** (không ciphertext, không raw Prisma error) và caller classify **transient** — không bị ngụy trang thành CAS loss, không bao giờ thành reconnect; (6) **worker-vs-worker stale overwrite đã bị chặn** (winner commit, loser discard có kiểm soát); (7) **worker cũ không thể overwrite successful OAuth reconnect** — OAuth connect/reconnect vẫn là authoritative human path và credential vừa reconnect được bảo toàn; (8) **late rotation không thể ghi credential vào mailbox `DISABLED`** (không resurrect, không đổi status); (9) enforcement tập trung ở **một shared seam** (`persistRotatedRefreshToken`) áp dụng cho renewal, delta polling, email worker và reconciliation — không duplicate CAS ở 4 nơi; (10) **TASK-084 Case A/B fail-closed behavior giữ nguyên** (CAS-lost → committed generation fallback về generation ban đầu → mailbox status writer fail-closed) và **TASK-069C / Graph classification giữ nguyên** (CAS loss không phải auth failure). **Không** schema/migration, Redis/distributed lock, transaction, OAuth redesign, env/CI/UI change. File liên quan: `docs/tasks/TASK-085-credential-rotation-concurrency-guard.md`, `docs/reports/TASK-085-credential-rotation-concurrency-guard.md`. **Remaining risk (theo task/report):** không còn LWW blind-overwrite ở tầng credential write giữa 4 worker và OAuth reconnect; residual đã chấp nhận theo external Microsoft Identity Platform semantics đã verify (nhiều refresh credential có thể đồng thời hợp lệ nên CAS-loser discard là an toàn); CAS predicate dựa trên exact-string equality của ciphertext trên PostgreSQL (đã dùng cho reconnect writer TASK-084) — nếu integration/live phát hiện lệch thì theo STOP condition của task. Không mở follow-up mới.

- TASK-086: Graph Subscription Provisioning Concurrency Guard — **implementation hoàn tất; Antigravity CLI Final Architecture Review PASS và Antigravity CLI Final Implementation Review PASS**. `npm run verify` PASS (exit 0, **104 test files / 1281 tests**, lint + typecheck + build sạch). **Nguồn gốc:** remaining risk mà TASK-081/082/083 đã ghi nhận — hai operation provisioning đồng thời (OAuth connect/reconnect, reconciliation apply, recovery apply) cùng đọc "chưa có subscription usable" rồi cùng tạo subscription. **Correctness invariant đã khóa:** tối đa **một local live GraphSubscription cho mỗi mailbox**, live statuses là `ACTIVE`/`RENEWING`/`FAILED`, enforce bằng **partial unique index trên `mailboxId`** (raw SQL migration theo precedent TASK-068A; index cố ý không biểu diễn trong `schema.prisma` vì Prisma không hỗ trợ partial unique index, có ghi chú drift cho maintainer). `resource` **không** thuộc correctness key. **Kết quả chính:** (1) DB là serialization point duy nhất — không Redis, không distributed lock, không long transaction/advisory lock xuyên HTTP, không placeholder row; (2) provisioning **normalize expired live state trước khi create**: row `ACTIVE`/`FAILED` đã quá hạn theo thời gian được chuyển `EXPIRED` bằng conditional write pinned theo row id + status đã đọc + `expirationDateTime <= capturedNow` (không unconditional update, không chạm `Mailbox.status`); (3) sau normalize, blocking definition là **bất kỳ row nào còn ở live status** — bỏ điều kiện `expirationDateTime > now` để application semantics khớp đúng partial unique index; (4) **fresh `RENEWING` giữ ownership**: claim còn trong stale window thì provisioning **tạm block** (không Graph call, không write, không retry ngay), **stale `RENEWING`** mới được transition với predicate concurrency-safe — dùng lại **đúng stale window 30 phút hiện có của TASK-084** qua một shared constant module, không tạo timeout thứ hai; (5) Microsoft `409` chỉ là **defensive signal, không phải local correctness guarantee** — implementation đúng ngay cả khi hai POST đồng thời đều nhận `201`; 409 có local live winner → controlled existing/conflict outcome, 409 không có local live winner → fail-safe (không fabricate row local, không flip mailbox `ACTIVE`, không blind retry); (6) **local unique conflict (P2002 trên live-slot index) được xử lý như ownership loss**: loser chỉ compensating-delete **remote subscription của chính nó** (không bao giờ cleanup theo `mailboxId`, không bao giờ chạm subscription của winner), re-read winner rồi trả controlled outcome, không retry Graph create; P2002 trên `subscriptionId` và mọi lỗi DB khác vẫn giữ **generic compensation/fail-open của TASK-081**; (7) **M2 migration strategy fail-closed** — migration **không** auto-deduplicate historical duplicate live rows, không tự chọn winner, không silently mutate lifecycle; nếu còn duplicate thì tạo index fail rõ ràng và rollout phải dừng, kèm preflight/verify query read-only trong comment migration. **Compatibility/regression PASS:** TASK-081 fail-open (mọi nhánh conflict/blocked/failure là outcome, OAuth connect/reconnect không rollback), TASK-082 dry-run vẫn strictly non-mutating (dry-run không gọi ensure nên normalization không thể write) và apply vẫn bounded/sequential, TASK-083 recovery chỉ flip `SUBSCRIPTION_EXPIRED → ACTIVE` khi local chứng minh có usable subscription — không flip vì 409/ownership loss và không cleanup winner, TASK-084 claim/CAS/stale reclaim nguyên vẹn (4 chiều race có test: claim thắng / normalization thắng / stale reclaim thắng / late completion CAS count 0), TASK-085 credential CAS nguyên vẹn. **Không** đổi UI, Telegram, detector/extractor, Redis wiring, `.env*` hay GitHub Actions. File liên quan: `docs/tasks/TASK-086-subscription-provisioning-concurrency-guard.md`, `docs/reports/TASK-086-subscription-provisioning-concurrency-guard.md`. **Remaining/deferred:** (1) capability discovery/cleanup cho orphan remote subscription vẫn out-of-scope, 409 chỉ được xử lý fail-safe; (2) migration **chưa** apply vào staging/production; (3) trước deploy phải chạy **read-only preflight** kiểm tra historical duplicate live rows — nếu có duplicate thì rollout dừng theo M2 và cần remediation có kiểm soát trước khi retry; (4) `resource` representation/canonicalization không phải correctness dependency hiện tại, deferred cho scope multi-resource tương lai; (5) fresh `RENEWING` có thể trì hoãn provisioning tới hết stale window khoảng 30 phút — intentional fail-safe, ưu tiên không double-create và không cướp renewal ownership.

- TASK-087: Staging Migration Preflight & Controlled Rollout Validation — **hoàn tất; Antigravity CLI Phase-1 Rollout Plan Review PASS và Antigravity CLI Final Staging Rollout Validation Review PASS**. `npm run verify` PASS (exit 0, **104 test files / 1281 tests**, lint + typecheck + build sạch). **Docs-only:** không sửa runtime code, schema, migration hay test; không thao tác production. **Nguồn gốc:** remaining item của TASK-086 — partial unique index chưa được apply vào môi trường thật và cần một rollout fail-closed có preflight, hậu kiểm và STOP conditions. **Chronology thực tế (ghi trung thực, không viết lại cho đẹp):** TASK-086 push → Railway staging automatic deploy → Pre-Deploy Command `npx prisma migrate deploy` tự chạy → migration TASK-086 apply thành công, web container Ready → TASK-087 phát hiện chính behavior automatic Pre-Deploy này → Human thực hiện post-deployment read-only verification. **Prospective preflight KHÔNG được chạy trước migration** vì automatic deploy đã apply trước khi Phase 2 bắt đầu; đây là **operational process observation**, không phải database correctness failure. **Kết quả xác minh (Human-observed, read-only trên staging):** (1) migration record TASK-086 ở trạng thái applied thành công (`finished_at` có giá trị, `applied_steps_count = 1`, không rolled back/failed); (2) partial unique index `GraphSubscription_mailboxId_live_unique` tồn tại đúng — UNIQUE, btree trên `mailboxId`, predicate đúng `ACTIVE`/`RENEWING`/`FAILED` (PostgreSQL hiển thị normalized `status = ANY (ARRAY[...])`, tương đương `IN (...)`) → enforce đúng invariant "một live GraphSubscription mỗi mailbox"; (3) duplicate check hiện tại theo live statuses trả về **0 violating mailboxes**; (4) `web`, `worker-email`, `worker-delta`, `worker-renewal` đều Active/Running; (5) core staging health checks (email worker pipeline, delta polling, queue/Redis, Telegram send reliability) không cho thấy deployment regression liên quan migration. Qua đó đóng **phần staging** của remaining item "migration chưa apply vào staging/production" ở dòng TASK-086; production vẫn chưa rollout. File liên quan: `docs/tasks/TASK-087-staging-migration-preflight-rollout-validation.md`, `docs/reports/TASK-087-staging-migration-preflight-rollout-validation.md`. **Remaining risks / lessons:** (1) với future migration có fail-closed data precondition, read-only preflight phải được thực hiện **trước cả hành động push/deploy** có thể trigger automatic Pre-Deploy migration, chứ không chỉ trước một lệnh migrate thủ công; (2) live Microsoft ownership-conflict/P2002 smoke **chưa** được thực hiện — không phải blocker của TASK-087; (3) reconciliation dry-run smoke **chưa** chạy — đánh giá non-blocking; (4) orphan remote subscription remediation vẫn **deferred** từ TASK-086; (5) một số staging mailbox operational issue vẫn tồn tại nhưng **không có evidence** liên hệ với migration TASK-086; (6) production rollout nằm **ngoài scope** TASK-087. Không tự gán task number mới cho follow-up — do Human/ChatGPT quyết định.

- TASK-088: Railway Staging Deployment Source Alignment & Migration Safety Guard — **hoàn tất investigation/architecture + controlled staging execution; Antigravity CLI Architecture Review PASS, Correction Re-review PASS, Phase 2 Plan Review PASS và Final Execution Review PASS**. `npm run verify` PASS (exit 0, **104 test files / 1281 tests**, lint + typecheck + build sạch). **Nguồn gốc:** operational process deviation ghi nhận ở TASK-087 — automatic Pre-Deploy migration chạy trước preflight vì staging theo dõi trực tiếp nhánh task với Auto Deploy bật. **Kết quả chính:** (1) cả 4 Railway staging service (web, worker-email, worker-delta, worker-renewal) hiện **cố định vào dedicated branch `staging`** thay vì trực tiếp theo nhánh task hay nhánh feature; branch `staging` được bootstrap tại đúng commit runtime TASK-086 đang deploy (zero-content-change khi đổi source model); (2) Auto Deploy vẫn ON và **Wait for CI đã bật ON** cho cả 4 service — GitHub Actions PASS rồi Railway mới deploy; (3) **web là sole Pre-Deploy migration executor** với Prisma migrate deploy; ba worker không chạy migration Pre-Deploy; (4) future promotion là **Human-controlled, fast-forward-only**, giữ invariant: approved deployment commit phải chính là promoted/deployed commit; task docs-only **không** bắt buộc promotion/redeploy; (5) future migration có data precondition phải tuân thủ **CASE 3 safety model / Mode A-B-C-D** đã khóa trong TASK-088 (precondition phải giữ đúng cho tới lúc migration enforce invariant); (6) Phase 2A–2D đều PASS: bootstrap dedicated staging branch, Wait-for-CI alignment, source switch từng service một, post-switch smoke validation; (7) core post-switch health: Email worker / Delta polling / Queue-Redis / Telegram reliability đều PASS — **không có observed regression attributable cho TASK-088**. File liên quan: `docs/tasks/TASK-088-railway-staging-source-alignment-migration-safety-guard.md`, `docs/reports/TASK-088-railway-staging-source-alignment-migration-safety-guard.md`. **Rủi ro còn lại / deferred:** Watch Paths chưa cấu hình — chỉ là optional operational optimization, hệ quả là một promotion hiện có thể trigger deploy cả 4 service; Graph 410 / SyncStateNotFound, một số mailbox RECONNECT_REQUIRED hoặc disabled, và renewal/webhook health UNKNOWN là **existing/independent operational observations** — KHÔNG phải regression của TASK-088 và KHÔNG được TASK-088 sửa; các runtime/migration task tương lai phải dùng **Deployment Impact Matrix** và migration safety rules của TASK-088. **Điều hướng task tiếp theo:** các task sau phải đọc TASK-088 trước khi quyết định deploy staging; ChatGPT/Human xác định service nào thực sự cần staging promotion — không phải mọi task đều cần redeploy; khi một runtime task được promotion, source của Railway **không** đổi sang task branch nữa — promotion đi qua dedicated branch `staging`. Không tự gán task number mới cho follow-up — do Human/ChatGPT quyết định.

- TASK-089: Microsoft Graph Delta 410 SyncStateNotFound Recovery & Cursor Resilience — **completed; Antigravity CLI Final Architecture Re-review PASS và Antigravity CLI Final Implementation Review PASS**. `npm run verify` PASS (exit 0, **105 test files / 1304 tests**, lint + typecheck + build sạch). **Root cause:** HTTP 410 trên delta request trước đây bị classify vào nhánh `unknown` — cursor invalid được giữ nguyên và **reuse qua từng scheduler tick vô hạn** (mỗi tick một token refresh + một Graph call vô ích) mà không có recovery path nào. **Kết quả chính (Option B++ replace-on-success):** (1) 410 được classify thành error kind sync-state riêng (theo HTTP status; Graph error code chỉ là diagnostics) và kích hoạt **same-cycle bounded recovery enumeration**; (2) invalid cursor **chỉ** bị thay khi recovery thành công — deltaLink mới ghi đè trực tiếp qua cursor writer sẵn có (đồng thời clear error metadata); (3) recovery fail (410 lặp/timeout/429/5xx) **giữ nguyên cursor cũ** làm durable trigger để tick sau tiếp tục đúng một probe + một recovery attempt — không recursion, không reset-null, không tight loop; (4) recovery lookback **bounded và tương thích freshness**: dẫn xuất từ **shared relay freshness policy 30 phút** (hằng số TASK-080 được extract sang leaf module `services/email/relay-freshness-policy.ts` — một nguồn sự thật, pipeline và delta cùng import, zero behavioral change cho pipeline); (5) **page-cap fail-safe**: chạm page cap chưa có deltaLink ⇒ recovery coi như thất bại, không persist intermediate cursor, không fabricate; (6) semantics **401/403 (TASK-071 cursor self-heal + TASK-075 forbidden backoff/alert) giữ nguyên hoàn toàn**, kể cả khi 401/403 xảy ra trong recovery (delegate nguyên văn); (7) **TASK-080 timeout 20s và stale guard 30 phút giữ nguyên** — recovery đi qua đúng fetch seam hiện có; (8) **Graph subscription/webhook không đổi**; (9) **không Prisma migration** — trạng thái recovery là in-memory trong cycle. Delivery semantics giữ wording chính xác: claim-before-send chặn duplicate replay cho cùng message identity, với pre-existing at-most-once-after-claim. File liên quan: `docs/tasks/TASK-089-delta-410-sync-state-recovery-cursor-resilience.md`, `docs/reports/TASK-089-delta-410-sync-state-recovery-cursor-resilience.md`. **Rủi ro còn lại:** nếu Graph recovery liên tục thất bại lâu hơn freshness window 30 phút, verification message cũ trong khoảng hở sẽ bị stale-skip đúng policy TASK-080 (tính chất an toàn có chủ đích, không phải lỗ hổng); pre-existing at-most-once-after-claim delivery failure semantics vẫn **deferred** — không phải regression của TASK-089. **Staging runtime validation: PASS (Antigravity CLI Staging Runtime Validation Review PASS).** Controlled fast-forward promotion vào dedicated branch `staging` theo quy trình TASK-088 thành công (staging HEAD = approved commit, GitHub Actions staging PASS, Railway source không đổi, CASE 1 — không migration); normal relay PASS (Email worker / Delta polling / Queue-Redis / Telegram reliability đều PASS, fresh verification email relay thành công, không historical-email replay bất thường); queue backlog tồn đọng **tự drain 2473 → 0** không cần restart/redeploy thủ công; **không phát hiện TASK-089 regression**. **Limitation ghi nhận trung thực:** trong observation window KHÔNG xuất hiện HTTP 410 tự nhiên từ provider — 410 recovery was validated deterministically (23 focused tests); no natural provider-side 410 occurred during the staging observation window; không tạo synthetic 410 bằng cách sửa DB cursor. **Independent observation:** một mailbox gặp HTTP 403 ErrorQuotaExceeded — investigation xác nhận thuộc existing TASK-071/TASK-075 forbidden path hoạt động đúng (counter/cooldown/alert), không phải TASK-089 regression, root cause provider-side cần Human kiểm tra quota ngoài repo — EXISTING / INDEPENDENT OPERATIONAL OBSERVATION, không mở scope. Không tự gán task number mới cho follow-up — do Human/ChatGPT quyết định.

- TASK-090: Post-Claim Telegram Delivery Failure Recovery & Delivery State Safety — **completed end-to-end, bao gồm staging rollout; Antigravity CLI Architecture Review PASS, Final Implementation Review PASS, Timeout Correction Re-review PASS, ROADMAP Close-out Review PASS và Staging Runtime Validation PASS**. `npm run verify` PASS (exit 0, **109 test files / 1344 tests**). **Root problem đã giải quyết:** pre-existing post-claim delivery loss (TASK-089 DF-1) — ProcessedMessage đã claim nhưng Telegram send fail/crash khiến BullMQ retry bị early dedup terminal-skip ⇒ verification code mất vĩnh viễn (S1/S2 chứng minh code-level rồi loại bỏ). **Kết quả chính:** post-claim Telegram delivery recovery (status-aware dedup: SENT/FAILED terminal, DETECTED chưa hoàn tất có bounded recovery); durable delivery ownership/CAS (lease 5 phút + conditional update, completion fenced bằng owner generation — stale owner không ghi đè owner mới); bounded retry (tối đa 3 delivery-ownership claims/message × ≤4 internal sends, chặn thêm bởi stale guard 30 phút được re-check trước mọi re-send; permanent Telegram failure đi thẳng FAILED terminal, 429 vẫn retryable); explicit Telegram HTTP timeout **15 giây** bằng AbortController cancellation thật (lease-budget margin ~43s được test pin). Không claim perfect exactly-once Telegram. **Staging rollout (CASE 2 TASK-088):** migration preflight read-only PASS → controlled ff-promotion vào dedicated branch `staging` tại reviewed SHA `707ca03d10879d170fa556fc18444665fdebd52b` → staging GitHub Actions PASS → **migration additive apply staging PASS** → post-deploy schema verification PASS → **runtime staging validation PASS** (4 Railway services Active; Email worker / Delta polling / Queue-Redis / Telegram reliability đều PASS; backlog = 0; không mass historical replay; không observed regression attributable to TASK-090). Queue FAILED retained count là historical BullMQ retention (recent failed = 0), Graph 403 ErrorQuotaExceeded và renewal/webhook UNKNOWN là existing/independent observations — TASK-090 không sửa các mục này. Final docs-only sync xảy ra SAU runtime validation, không cần promotion. File liên quan: `docs/tasks/TASK-090-post-claim-delivery-recovery.md`, `docs/reports/TASK-090-post-claim-delivery-recovery.md`. **Rủi ro còn lại:** ambiguous remote-success/lost-ack duplicate window (provider contract); Telegram message-id reconciliation deferred; stale failure reason health-detail improvement deferred.
